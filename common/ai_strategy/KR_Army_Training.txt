# Written by Henrik "Groogy" Hansson
# Modified by Daniel "Da9L" Sjöberg

# befriend
# conquer
# antagonize
# build_ship
# build_army
# unit_ratio
# build_building
# research_tech
# garrison
# protect
# influence
# contain
# area_priority

ai_army_building_minimum_puppet = { #Minimum Army Size Enforced - Puppets
	enable = {
		has_army_size = { size < 5 }
		is_subject = yes
		NOT = { original_tag = SWI }
		NOT = { original_tag = TIB }
		NOT = { original_tag = NEP }
		NOT = { original_tag = BHU }
		NOT = { original_tag = AFG }
		NOT = { original_tag = PER }
		NOT = { original_tag = AZB }
		NOT = { original_tag = ARM }
		NOT = { original_tag = GEO }
		NOT = { original_tag = ITA }
		NOT = { original_tag = AUS }
		NOT = { original_tag = NOR }
		NOT = { original_tag = PRU }
		NOT = { original_tag = CHL }
		NOT = { original_tag = OTT }
		NOT = { original_tag = YUN }
		NOT = { original_tag = XSM }
		NOT = { original_tag = SRI }
		NOT = { original_tag = ETH }
		NOT = { original_tag = MEX }
		NOT = { original_tag = COL }
		NOT = { original_tag = BOL }
		NOT = { original_tag = ARG }
		NOT = { original_tag = HUN }
		NOT = { original_tag = GRE }
		NOT = { original_tag = BUL }
		NOT = { original_tag = POR }
		NOT = { original_tag = SWE }
		NOT = { original_tag = PHL }
		NOT = { original_tag = USA }
		NOT = { original_tag = CSA }
		NOT = { original_tag = CAL }
		NOT = { original_tag = TEX }
		NOT = { original_tag = CUB }
		NOT = { original_tag = HAI }
		NOT = { original_tag = DOM }
		NOT = { original_tag = CAN }
		NOT = { original_tag = MAF }
		NOT = { original_tag = GER }
		NOT = { original_tag = AOG }
		NOT = { original_tag = CAF }
		NOT = { original_tag = NFA }
		NOT = { original_tag = JAP }
		NOT = { original_tag = TRM }
		NOT = { original_tag = AST }
		NOT = { original_tag = HOL }
		NOT = { original_tag = CRE }
		NOT = { original_tag = ENG }
		NOT = { original_tag = IRE }
		NOT = { original_tag = SPR }
		NOT = { original_tag = CAR }
		NOT = { original_tag = CNT }
		NOT = { original_tag = MON }
		NOT = { original_tag = ALO }
		NOT = { original_tag = TRK }
		NOT = { original_tag = DKB }
		NOT = { original_tag = SAU }
		NOT = { original_tag = YEM }
		NOT = { original_tag = OMA }
	}	
	abort = {
		OR = {
			NOT = {has_army_size = { size < 5 } }
			has_war = yes
			is_subject = no
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 5		
	}
}

ai_army_building_minimum = { #Minimum Army Size Enforced
	enable = {
		has_army_size = { size < 10 }
		has_war = no
		is_subject = no
		NOT = { original_tag = SWI }
		NOT = { original_tag = TIB }
		NOT = { original_tag = NEP }
		NOT = { original_tag = BHU }
		NOT = { original_tag = AFG }
		NOT = { original_tag = PER }
		NOT = { original_tag = AZB }
		NOT = { original_tag = ARM }
		NOT = { original_tag = GEO }
		NOT = { original_tag = ITA }
		NOT = { original_tag = AUS }
		NOT = { original_tag = NOR }
		NOT = { original_tag = PRU }
		NOT = { original_tag = CHL }
		NOT = { original_tag = OTT }
		NOT = { original_tag = YUN }
		NOT = { original_tag = XSM }
		NOT = { original_tag = SRI }
		NOT = { original_tag = ETH }
		NOT = { original_tag = MEX }
		NOT = { original_tag = COL }
		NOT = { original_tag = BOL }
		NOT = { original_tag = ARG }
		NOT = { original_tag = HUN }
		NOT = { original_tag = GRE }
		NOT = { original_tag = BUL }
		NOT = { original_tag = POR }
		NOT = { original_tag = SWE }
		NOT = { original_tag = PHL }
		NOT = { original_tag = USA }
		NOT = { original_tag = CSA }
		NOT = { original_tag = CAL }
		NOT = { original_tag = TEX }
		NOT = { original_tag = CUB }
		NOT = { original_tag = HAI }
		NOT = { original_tag = DOM }
		NOT = { original_tag = CAN }
		NOT = { original_tag = MAF }
		NOT = { original_tag = GER }
		NOT = { original_tag = AOG }
		NOT = { original_tag = CAF }
		NOT = { original_tag = NFA }
		NOT = { original_tag = JAP }
		NOT = { original_tag = TRM }
		NOT = { original_tag = AST }
		NOT = { original_tag = HOL }
		NOT = { original_tag = CRE }
		NOT = { original_tag = ENG }
		NOT = { original_tag = IRE }
		NOT = { original_tag = SPR }
		NOT = { original_tag = CAR }
		NOT = { original_tag = CNT }
		NOT = { original_tag = MON }
		NOT = { original_tag = ALO }
		NOT = { original_tag = TRK }
		NOT = { original_tag = DKB }
		NOT = { original_tag = SAU }
		NOT = { original_tag = YEM }
		NOT = { original_tag = OMA }
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 10 } }
			has_war = yes
			is_subject = yes
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 10		
	}
}

ai_army_building_20 = { #20 Units
	enable = {
		ai_wants_divisions < 20
		has_army_size = { size < 20 }
		has_war = no
		num_of_factories > 9
		num_of_factories < 19
		has_manpower > 50000
		NOT = { original_tag = SWI }
		NOT = { original_tag = TIB }
		NOT = { original_tag = NEP }
		NOT = { original_tag = BHU }
		NOT = { original_tag = AFG }
		NOT = { original_tag = PER }
		NOT = { original_tag = AZB }
		NOT = { original_tag = ARM }
		NOT = { original_tag = GEO }
		NOT = { original_tag = ITA }
		NOT = { original_tag = AUS }
		NOT = { original_tag = NOR }
		NOT = { original_tag = PRU }
		NOT = { original_tag = CHL }
		NOT = { original_tag = OTT }
		NOT = { original_tag = YUN }
		NOT = { original_tag = XSM }
		NOT = { original_tag = SRI }
		NOT = { original_tag = ETH }
		NOT = { original_tag = MEX }
		NOT = { original_tag = COL }
		NOT = { original_tag = BOL }
		NOT = { original_tag = ARG }
		NOT = { original_tag = HUN }
		NOT = { original_tag = GRE }
		NOT = { original_tag = BUL }
		NOT = { original_tag = POR }
		NOT = { original_tag = SWE }
		NOT = { original_tag = PHL }
		NOT = { original_tag = USA }
		NOT = { original_tag = CSA }
		NOT = { original_tag = CAL }
		NOT = { original_tag = TEX }
		NOT = { original_tag = CUB }
		NOT = { original_tag = HAI }
		NOT = { original_tag = DOM }
		NOT = { original_tag = CAN }
		NOT = { original_tag = MAF }
		NOT = { original_tag = GER }
		NOT = { original_tag = AOG }
		NOT = { original_tag = CAF }
		NOT = { original_tag = NFA }
		NOT = { original_tag = JAP }
		NOT = { original_tag = TRM }
		NOT = { original_tag = AST }
		NOT = { original_tag = HOL }
		NOT = { original_tag = CRE }
		NOT = { original_tag = ENG }
		NOT = { original_tag = IRE }
		NOT = { original_tag = SPR }
		NOT = { original_tag = CAR }
		NOT = { original_tag = CNT }
		NOT = { original_tag = MON }
		NOT = { original_tag = ALO }
		NOT = { original_tag = TRK }
		NOT = { original_tag = DKB }
		NOT = { original_tag = SAU }
		NOT = { original_tag = YEM }
		NOT = { original_tag = OMA }
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 20 } }
			has_war = yes
			NOT = { num_of_factories > 9 }
			NOT = { num_of_factories < 19 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 20		
	}
}

ai_army_building_30 = { #30 Units
	enable = {
		ai_wants_divisions < 30
		has_army_size = { size < 30 }
		has_war = no
		num_of_factories > 19
		num_of_factories < 29
		has_manpower > 50000
		NOT = { original_tag = SWI }
		NOT = { original_tag = TIB }
		NOT = { original_tag = NEP }
		NOT = { original_tag = BHU }
		NOT = { original_tag = AFG }
		NOT = { original_tag = PER }
		NOT = { original_tag = AZB }
		NOT = { original_tag = ARM }
		NOT = { original_tag = GEO }
		NOT = { original_tag = ITA }
		NOT = { original_tag = AUS }
		NOT = { original_tag = NOR }
		NOT = { original_tag = PRU }
		NOT = { original_tag = CHL }
		NOT = { original_tag = OTT }
		NOT = { original_tag = YUN }
		NOT = { original_tag = XSM }
		NOT = { original_tag = SRI }
		NOT = { original_tag = ETH }
		NOT = { original_tag = MEX }
		NOT = { original_tag = COL }
		NOT = { original_tag = BOL }
		NOT = { original_tag = ARG }
		NOT = { original_tag = HUN }
		NOT = { original_tag = GRE }
		NOT = { original_tag = BUL }
		NOT = { original_tag = POR }
		NOT = { original_tag = SWE }
		NOT = { original_tag = PHL }
		NOT = { original_tag = USA }
		NOT = { original_tag = CSA }
		NOT = { original_tag = CAL }
		NOT = { original_tag = TEX }
		NOT = { original_tag = CUB }
		NOT = { original_tag = HAI }
		NOT = { original_tag = DOM }
		NOT = { original_tag = CAN }
		NOT = { original_tag = MAF }
		NOT = { original_tag = GER }
		NOT = { original_tag = AOG }
		NOT = { original_tag = CAF }
		NOT = { original_tag = NFA }
		NOT = { original_tag = JAP }
		NOT = { original_tag = TRM }
		NOT = { original_tag = AST }
		NOT = { original_tag = HOL }
		NOT = { original_tag = CRE }
		NOT = { original_tag = ENG }
		NOT = { original_tag = IRE }
		NOT = { original_tag = SPR }
		NOT = { original_tag = CAR }
		NOT = { original_tag = CNT }
		NOT = { original_tag = MON }
		NOT = { original_tag = ALO }
		NOT = { original_tag = TRK }
		NOT = { original_tag = DKB }
		NOT = { original_tag = SAU }
		NOT = { original_tag = YEM }
		NOT = { original_tag = OMA }
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 30 } }
			has_war = yes
			NOT = { num_of_factories > 19 }
			NOT = { num_of_factories < 29 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 30		
	}
}

ai_army_building_40 = { #40 Units
	enable = {
		ai_wants_divisions < 40
		has_army_size = { size < 40 }
		has_war = no
		num_of_factories > 29
		num_of_factories < 39
		has_manpower > 50000
		NOT = { original_tag = SWI }
		NOT = { original_tag = TIB }
		NOT = { original_tag = NEP }
		NOT = { original_tag = BHU }
		NOT = { original_tag = AFG }
		NOT = { original_tag = PER }
		NOT = { original_tag = AZB }
		NOT = { original_tag = ARM }
		NOT = { original_tag = GEO }
		NOT = { original_tag = ITA }
		NOT = { original_tag = AUS }
		NOT = { original_tag = NOR }
		NOT = { original_tag = PRU }
		NOT = { original_tag = CHL }
		NOT = { original_tag = OTT }
		NOT = { original_tag = YUN }
		NOT = { original_tag = XSM }
		NOT = { original_tag = SRI }
		NOT = { original_tag = ETH }
		NOT = { original_tag = MEX }
		NOT = { original_tag = COL }
		NOT = { original_tag = BOL }
		NOT = { original_tag = ARG }
		NOT = { original_tag = HUN }
		NOT = { original_tag = GRE }
		NOT = { original_tag = BUL }
		NOT = { original_tag = POR }
		NOT = { original_tag = SWE }
		NOT = { original_tag = PHL }
		NOT = { original_tag = USA }
		NOT = { original_tag = CSA }
		NOT = { original_tag = CAL }
		NOT = { original_tag = TEX }
		NOT = { original_tag = CUB }
		NOT = { original_tag = HAI }
		NOT = { original_tag = DOM }
		NOT = { original_tag = CAN }
		NOT = { original_tag = MAF }
		NOT = { original_tag = GER }
		NOT = { original_tag = AOG }
		NOT = { original_tag = CAF }
		NOT = { original_tag = NFA }
		NOT = { original_tag = JAP }
		NOT = { original_tag = TRM }
		NOT = { original_tag = AST }
		NOT = { original_tag = HOL }
		NOT = { original_tag = CRE }
		NOT = { original_tag = ENG }
		NOT = { original_tag = IRE }
		NOT = { original_tag = SPR }
		NOT = { original_tag = CAR }
		NOT = { original_tag = CNT }
		NOT = { original_tag = MON }
		NOT = { original_tag = ALO }
		NOT = { original_tag = TRK }
		NOT = { original_tag = DKB }
		NOT = { original_tag = SAU }
		NOT = { original_tag = YEM }
		NOT = { original_tag = OMA }
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 40 } }
			has_war = yes
			NOT = { num_of_factories > 29 }
			NOT = { num_of_factories < 39 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 40		
	}
}

ai_army_building_50 = { #50 Units
	enable = {
		ai_wants_divisions < 50
		has_army_size = { size < 50 }
		has_war = no
		num_of_factories > 39
		num_of_factories < 49
		has_manpower > 50000
		NOT = { original_tag = SWI }
		NOT = { original_tag = TIB }
		NOT = { original_tag = NEP }
		NOT = { original_tag = BHU }
		NOT = { original_tag = AFG }
		NOT = { original_tag = PER }
		NOT = { original_tag = AZB }
		NOT = { original_tag = ARM }
		NOT = { original_tag = GEO }
		NOT = { original_tag = ITA }
		NOT = { original_tag = AUS }
		NOT = { original_tag = NOR }
		NOT = { original_tag = PRU }
		NOT = { original_tag = CHL }
		NOT = { original_tag = OTT }
		NOT = { original_tag = YUN }
		NOT = { original_tag = XSM }
		NOT = { original_tag = SRI }
		NOT = { original_tag = ETH }
		NOT = { original_tag = MEX }
		NOT = { original_tag = COL }
		NOT = { original_tag = BOL }
		NOT = { original_tag = ARG }
		NOT = { original_tag = HUN }
		NOT = { original_tag = GRE }
		NOT = { original_tag = BUL }
		NOT = { original_tag = POR }
		NOT = { original_tag = SWE }
		NOT = { original_tag = PHL }
		NOT = { original_tag = USA }
		NOT = { original_tag = CSA }
		NOT = { original_tag = CAL }
		NOT = { original_tag = TEX }
		NOT = { original_tag = CUB }
		NOT = { original_tag = HAI }
		NOT = { original_tag = DOM }
		NOT = { original_tag = CAN }
		NOT = { original_tag = MAF }
		NOT = { original_tag = GER }
		NOT = { original_tag = AOG }
		NOT = { original_tag = CAF }
		NOT = { original_tag = NFA }
		NOT = { original_tag = JAP }
		NOT = { original_tag = TRM }
		NOT = { original_tag = AST }
		NOT = { original_tag = HOL }
		NOT = { original_tag = CRE }
		NOT = { original_tag = ENG }
		NOT = { original_tag = IRE }
		NOT = { original_tag = SPR }
		NOT = { original_tag = CAR }
		NOT = { original_tag = CNT }
		NOT = { original_tag = MON }
		NOT = { original_tag = ALO }
		NOT = { original_tag = TRK }
		NOT = { original_tag = DKB }
		NOT = { original_tag = SAU }
		NOT = { original_tag = YEM }
		NOT = { original_tag = OMA }
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 50 } }
			has_war = yes
			NOT = { num_of_factories > 39 }
			NOT = { num_of_factories < 49 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 50		
	}
}

ai_army_building_60 = { #60 Units
	enable = {
		ai_wants_divisions < 60
		has_army_size = { size < 60 }
		has_war = no
		num_of_factories > 49
		num_of_factories < 59
		has_manpower > 50000
		NOT = { original_tag = SWI }
		NOT = { original_tag = TIB }
		NOT = { original_tag = NEP }
		NOT = { original_tag = BHU }
		NOT = { original_tag = AFG }
		NOT = { original_tag = PER }
		NOT = { original_tag = AZB }
		NOT = { original_tag = ARM }
		NOT = { original_tag = GEO }
		NOT = { original_tag = ITA }
		NOT = { original_tag = AUS }
		NOT = { original_tag = NOR }
		NOT = { original_tag = PRU }
		NOT = { original_tag = CHL }
		NOT = { original_tag = OTT }
		NOT = { original_tag = YUN }
		NOT = { original_tag = XSM }
		NOT = { original_tag = SRI }
		NOT = { original_tag = ETH }
		NOT = { original_tag = MEX }
		NOT = { original_tag = COL }
		NOT = { original_tag = BOL }
		NOT = { original_tag = ARG }
		NOT = { original_tag = HUN }
		NOT = { original_tag = GRE }
		NOT = { original_tag = BUL }
		NOT = { original_tag = POR }
		NOT = { original_tag = SWE }
		NOT = { original_tag = PHL }
		NOT = { original_tag = USA }
		NOT = { original_tag = CSA }
		NOT = { original_tag = CAL }
		NOT = { original_tag = TEX }
		NOT = { original_tag = CUB }
		NOT = { original_tag = HAI }
		NOT = { original_tag = DOM }
		NOT = { original_tag = CAN }
		NOT = { original_tag = MAF }
		NOT = { original_tag = GER }
		NOT = { original_tag = AOG }
		NOT = { original_tag = CAF }
		NOT = { original_tag = NFA }
		NOT = { original_tag = JAP }
		NOT = { original_tag = TRM }
		NOT = { original_tag = AST }
		NOT = { original_tag = HOL }
		NOT = { original_tag = CRE }
		NOT = { original_tag = ENG }
		NOT = { original_tag = IRE }
		NOT = { original_tag = SPR }
		NOT = { original_tag = CAR }
		NOT = { original_tag = CNT }
		NOT = { original_tag = MON }
		NOT = { original_tag = ALO }
		NOT = { original_tag = TRK }
		NOT = { original_tag = DKB }
		NOT = { original_tag = SAU }
		NOT = { original_tag = YEM }
		NOT = { original_tag = OMA }
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 60 } }
			has_war = yes
			NOT = { num_of_factories > 49 }
			NOT = { num_of_factories < 59 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 60		
	}
}

ai_army_building_70 = { #70 Units
	enable = {
		ai_wants_divisions < 70
		has_army_size = { size < 70 }
		has_war = no
		num_of_factories > 59
		num_of_factories < 69
		has_manpower > 50000
		NOT = { original_tag = SWI }
		NOT = { original_tag = TIB }
		NOT = { original_tag = NEP }
		NOT = { original_tag = BHU }
		NOT = { original_tag = AFG }
		NOT = { original_tag = PER }
		NOT = { original_tag = AZB }
		NOT = { original_tag = ARM }
		NOT = { original_tag = GEO }
		NOT = { original_tag = ITA }
		NOT = { original_tag = AUS }
		NOT = { original_tag = NOR }
		NOT = { original_tag = PRU }
		NOT = { original_tag = CHL }
		NOT = { original_tag = OTT }
		NOT = { original_tag = YUN }
		NOT = { original_tag = XSM }
		NOT = { original_tag = SRI }
		NOT = { original_tag = ETH }
		NOT = { original_tag = MEX }
		NOT = { original_tag = COL }
		NOT = { original_tag = BOL }
		NOT = { original_tag = ARG }
		NOT = { original_tag = HUN }
		NOT = { original_tag = GRE }
		NOT = { original_tag = BUL }
		NOT = { original_tag = POR }
		NOT = { original_tag = SWE }
		NOT = { original_tag = PHL }
		NOT = { original_tag = USA }
		NOT = { original_tag = CSA }
		NOT = { original_tag = CAL }
		NOT = { original_tag = TEX }
		NOT = { original_tag = CUB }
		NOT = { original_tag = HAI }
		NOT = { original_tag = DOM }
		NOT = { original_tag = CAN }
		NOT = { original_tag = MAF }
		NOT = { original_tag = GER }
		NOT = { original_tag = AOG }
		NOT = { original_tag = CAF }
		NOT = { original_tag = NFA }
		NOT = { original_tag = JAP }
		NOT = { original_tag = TRM }
		NOT = { original_tag = AST }
		NOT = { original_tag = HOL }
		NOT = { original_tag = CRE }
		NOT = { original_tag = ENG }
		NOT = { original_tag = IRE }
		NOT = { original_tag = SPR }
		NOT = { original_tag = CAR }
		NOT = { original_tag = CNT }
		NOT = { original_tag = MON }
		NOT = { original_tag = ALO }
		NOT = { original_tag = TRK }
		NOT = { original_tag = DKB }
		NOT = { original_tag = SAU }
		NOT = { original_tag = YEM }
		NOT = { original_tag = OMA }
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 70 } }
			has_war = yes
			NOT = { num_of_factories > 59 }
			NOT = { num_of_factories < 69 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 70		
	}
}

ai_army_building_80 = { #80 Units
	enable = {
		ai_wants_divisions < 80
		has_army_size = { size < 80 }
		has_war = no
		num_of_factories > 69
		num_of_factories < 79
		has_manpower > 50000
		NOT = { original_tag = SWI }
		NOT = { original_tag = TIB }
		NOT = { original_tag = NEP }
		NOT = { original_tag = BHU }
		NOT = { original_tag = AFG }
		NOT = { original_tag = PER }
		NOT = { original_tag = AZB }
		NOT = { original_tag = ARM }
		NOT = { original_tag = GEO }
		NOT = { original_tag = ITA }
		NOT = { original_tag = AUS }
		NOT = { original_tag = NOR }
		NOT = { original_tag = PRU }
		NOT = { original_tag = CHL }
		NOT = { original_tag = OTT }
		NOT = { original_tag = YUN }
		NOT = { original_tag = XSM }
		NOT = { original_tag = SRI }
		NOT = { original_tag = ETH }
		NOT = { original_tag = MEX }
		NOT = { original_tag = COL }
		NOT = { original_tag = BOL }
		NOT = { original_tag = ARG }
		NOT = { original_tag = HUN }
		NOT = { original_tag = GRE }
		NOT = { original_tag = BUL }
		NOT = { original_tag = POR }
		NOT = { original_tag = SWE }
		NOT = { original_tag = PHL }
		NOT = { original_tag = USA }
		NOT = { original_tag = CSA }
		NOT = { original_tag = CAL }
		NOT = { original_tag = TEX }
		NOT = { original_tag = CUB }
		NOT = { original_tag = HAI }
		NOT = { original_tag = DOM }
		NOT = { original_tag = CAN }
		NOT = { original_tag = MAF }
		NOT = { original_tag = GER }
		NOT = { original_tag = AOG }
		NOT = { original_tag = CAF }
		NOT = { original_tag = NFA }
		NOT = { original_tag = JAP }
		NOT = { original_tag = TRM }
		NOT = { original_tag = AST }
		NOT = { original_tag = HOL }
		NOT = { original_tag = CRE }
		NOT = { original_tag = ENG }
		NOT = { original_tag = IRE }
		NOT = { original_tag = SPR }
		NOT = { original_tag = CAR }
		NOT = { original_tag = CNT }
		NOT = { original_tag = MON }
		NOT = { original_tag = ALO }
		NOT = { original_tag = TRK }
		NOT = { original_tag = DKB }
		NOT = { original_tag = SAU }
		NOT = { original_tag = YEM }
		NOT = { original_tag = OMA }
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 80 } }
			has_war = yes
			NOT = { num_of_factories > 69 }
			NOT = { num_of_factories < 79 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 80		
	}
}

ai_army_building_90 = { #90 Units
	enable = {
		ai_wants_divisions < 90
		has_army_size = { size < 90 }
		has_war = no
		num_of_factories > 79
		num_of_factories < 89
		has_manpower > 50000
		NOT = { original_tag = SWI }
		NOT = { original_tag = TIB }
		NOT = { original_tag = NEP }
		NOT = { original_tag = BHU }
		NOT = { original_tag = AFG }
		NOT = { original_tag = PER }
		NOT = { original_tag = AZB }
		NOT = { original_tag = ARM }
		NOT = { original_tag = GEO }
		NOT = { original_tag = ITA }
		NOT = { original_tag = AUS }
		NOT = { original_tag = NOR }
		NOT = { original_tag = PRU }
		NOT = { original_tag = CHL }
		NOT = { original_tag = OTT }
		NOT = { original_tag = YUN }
		NOT = { original_tag = XSM }
		NOT = { original_tag = SRI }
		NOT = { original_tag = ETH }
		NOT = { original_tag = MEX }
		NOT = { original_tag = COL }
		NOT = { original_tag = BOL }
		NOT = { original_tag = ARG }
		NOT = { original_tag = HUN }
		NOT = { original_tag = GRE }
		NOT = { original_tag = BUL }
		NOT = { original_tag = POR }
		NOT = { original_tag = SWE }
		NOT = { original_tag = PHL }
		NOT = { original_tag = USA }
		NOT = { original_tag = CSA }
		NOT = { original_tag = CAL }
		NOT = { original_tag = TEX }
		NOT = { original_tag = CUB }
		NOT = { original_tag = HAI }
		NOT = { original_tag = DOM }
		NOT = { original_tag = CAN }
		NOT = { original_tag = MAF }
		NOT = { original_tag = GER }
		NOT = { original_tag = AOG }
		NOT = { original_tag = CAF }
		NOT = { original_tag = NFA }
		NOT = { original_tag = JAP }
		NOT = { original_tag = TRM }
		NOT = { original_tag = AST }
		NOT = { original_tag = HOL }
		NOT = { original_tag = CRE }
		NOT = { original_tag = ENG }
		NOT = { original_tag = IRE }
		NOT = { original_tag = SPR }
		NOT = { original_tag = CAR }
		NOT = { original_tag = CNT }
		NOT = { original_tag = MON }
		NOT = { original_tag = ALO }
		NOT = { original_tag = TRK }
		NOT = { original_tag = DKB }
		NOT = { original_tag = SAU }
		NOT = { original_tag = YEM }
		NOT = { original_tag = OMA }
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 90 } }
			has_war = yes
			NOT = { num_of_factories > 79 }
			NOT = { num_of_factories < 89 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 90		
	}
}

ai_army_building_100 = { #100 Units
	enable = {
		ai_wants_divisions < 100
		has_army_size = { size < 100 }
		has_war = no
		num_of_factories > 89
		num_of_factories < 99
		has_manpower > 50000
		NOT = { original_tag = SWI }
		NOT = { original_tag = TIB }
		NOT = { original_tag = NEP }
		NOT = { original_tag = BHU }
		NOT = { original_tag = AFG }
		NOT = { original_tag = PER }
		NOT = { original_tag = AZB }
		NOT = { original_tag = ARM }
		NOT = { original_tag = GEO }
		NOT = { original_tag = ITA }
		NOT = { original_tag = AUS }
		NOT = { original_tag = NOR }
		NOT = { original_tag = PRU }
		NOT = { original_tag = CHL }
		NOT = { original_tag = OTT }
		NOT = { original_tag = YUN }
		NOT = { original_tag = XSM }
		NOT = { original_tag = SRI }
		NOT = { original_tag = ETH }
		NOT = { original_tag = MEX }
		NOT = { original_tag = COL }
		NOT = { original_tag = BOL }
		NOT = { original_tag = ARG }
		NOT = { original_tag = HUN }
		NOT = { original_tag = GRE }
		NOT = { original_tag = BUL }
		NOT = { original_tag = POR }
		NOT = { original_tag = SWE }
		NOT = { original_tag = PHL }
		NOT = { original_tag = USA }
		NOT = { original_tag = CSA }
		NOT = { original_tag = CAL }
		NOT = { original_tag = TEX }
		NOT = { original_tag = CUB }
		NOT = { original_tag = HAI }
		NOT = { original_tag = DOM }
		NOT = { original_tag = CAN }
		NOT = { original_tag = MAF }
		NOT = { original_tag = GER }
		NOT = { original_tag = AOG }
		NOT = { original_tag = CAF }
		NOT = { original_tag = NFA }
		NOT = { original_tag = JAP }
		NOT = { original_tag = TRM }
		NOT = { original_tag = AST }
		NOT = { original_tag = HOL }
		NOT = { original_tag = CRE }
		NOT = { original_tag = ENG }
		NOT = { original_tag = IRE }
		NOT = { original_tag = SPR }
		NOT = { original_tag = CAR }
		NOT = { original_tag = CNT }
		NOT = { original_tag = MON }
		NOT = { original_tag = ALO }
		NOT = { original_tag = TRK }
		NOT = { original_tag = DKB }
		NOT = { original_tag = SAU }
		NOT = { original_tag = YEM }
		NOT = { original_tag = OMA }
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 100 } }
			has_war = yes
			NOT = { num_of_factories > 89 }
			NOT = { num_of_factories < 99 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 100		
	}
}

ai_army_building_150 = { #150 Units
	enable = {
		ai_wants_divisions < 150
		has_army_size = { size < 150 }
		has_war = no
		num_of_factories > 99
		has_manpower > 50000
		NOT = { original_tag = SWI }
		NOT = { original_tag = TIB }
		NOT = { original_tag = NEP }
		NOT = { original_tag = BHU }
		NOT = { original_tag = AFG }
		NOT = { original_tag = PER }
		NOT = { original_tag = AZB }
		NOT = { original_tag = ARM }
		NOT = { original_tag = GEO }
		NOT = { original_tag = ITA }
		NOT = { original_tag = AUS }
		NOT = { original_tag = NOR }
		NOT = { original_tag = PRU }
		NOT = { original_tag = CHL }
		NOT = { original_tag = OTT }
		NOT = { original_tag = YUN }
		NOT = { original_tag = XSM }
		NOT = { original_tag = SRI }
		NOT = { original_tag = ETH }
		NOT = { original_tag = MEX }
		NOT = { original_tag = COL }
		NOT = { original_tag = BOL }
		NOT = { original_tag = ARG }
		NOT = { original_tag = HUN }
		NOT = { original_tag = GRE }
		NOT = { original_tag = BUL }
		NOT = { original_tag = POR }
		NOT = { original_tag = SWE }
		NOT = { original_tag = PHL }
		NOT = { original_tag = USA }
		NOT = { original_tag = CSA }
		NOT = { original_tag = CAL }
		NOT = { original_tag = TEX }
		NOT = { original_tag = CUB }
		NOT = { original_tag = HAI }
		NOT = { original_tag = DOM }
		NOT = { original_tag = CAN }
		NOT = { original_tag = MAF }
		NOT = { original_tag = GER }
		NOT = { original_tag = AOG }
		NOT = { original_tag = CAF }
		NOT = { original_tag = NFA }
		NOT = { original_tag = JAP }
		NOT = { original_tag = TRM }
		NOT = { original_tag = AST }
		NOT = { original_tag = HOL }
		NOT = { original_tag = CRE }
		NOT = { original_tag = ENG }
		NOT = { original_tag = IRE }
		NOT = { original_tag = SPR }
		NOT = { original_tag = CAR }
		NOT = { original_tag = CNT }
		NOT = { original_tag = MON }
		NOT = { original_tag = ALO }
		NOT = { original_tag = TRK }
		NOT = { original_tag = DKB }
		NOT = { original_tag = SAU }
		NOT = { original_tag = YEM }
		NOT = { original_tag = OMA }
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 150 } }
			has_war = yes
			NOT = { num_of_factories > 99 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 150		
	}
}

ai_army_building_300 = { # Please Stop Building Units
	enable = {
		NOT = {ai_wants_divisions < 300}
		NOT = {has_army_size = { size < 300 }}
		has_war = no
		num_of_factories > 99
		has_manpower > 50000
		NOT = { original_tag = SWI }
		NOT = { original_tag = TIB }
		NOT = { original_tag = NEP }
		NOT = { original_tag = BHU }
		NOT = { original_tag = AFG }
		NOT = { original_tag = PER }
		NOT = { original_tag = AZB }
		NOT = { original_tag = ARM }
		NOT = { original_tag = GEO }
		NOT = { original_tag = ITA }
		NOT = { original_tag = AUS }
		NOT = { original_tag = NOR }
		NOT = { original_tag = PRU }
		NOT = { original_tag = CHL }
		NOT = { original_tag = OTT }
		NOT = { original_tag = YUN }
		NOT = { original_tag = XSM }
		NOT = { original_tag = SRI }
		NOT = { original_tag = ETH }
		NOT = { original_tag = MEX }
		NOT = { original_tag = COL }
		NOT = { original_tag = BOL }
		NOT = { original_tag = ARG }
		NOT = { original_tag = HUN }
		NOT = { original_tag = GRE }
		NOT = { original_tag = BUL }
		NOT = { original_tag = POR }
		NOT = { original_tag = SWE }
		NOT = { original_tag = PHL }
		NOT = { original_tag = USA }
		NOT = { original_tag = CSA }
		NOT = { original_tag = CAL }
		NOT = { original_tag = TEX }
		NOT = { original_tag = CUB }
		NOT = { original_tag = HAI }
		NOT = { original_tag = DOM }
		NOT = { original_tag = CAN }
		NOT = { original_tag = MAF }
		NOT = { original_tag = GER }
		NOT = { original_tag = AOG }
		NOT = { original_tag = CAF }
		NOT = { original_tag = NFA }
		NOT = { original_tag = JAP }
		NOT = { original_tag = TRM }
		NOT = { original_tag = AST }
		NOT = { original_tag = HOL }
		NOT = { original_tag = CRE }
		NOT = { original_tag = ENG }
		NOT = { original_tag = IRE }
		NOT = { original_tag = SPR }
		NOT = { original_tag = CAR }
		NOT = { original_tag = CNT }
		NOT = { original_tag = MON }
		NOT = { original_tag = ALO }
		NOT = { original_tag = TRK }
		NOT = { original_tag = DKB }
		NOT = { original_tag = SAU }
		NOT = { original_tag = YEM }
		NOT = { original_tag = OMA }
	}	
	abort = { 
		OR = {
			has_army_size = { size < 300 }
			NOT = {num_of_factories > 99}
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 0		
	}
}

ai_army_building_small_stockpile = { # Build if you're sitting on too many guns
	enable = {
		ai_wants_divisions < 50
		has_army_size = { size < 50 }
		has_equipment = { infantry_equipment > 20000 } 
		has_war = no
		has_manpower > 50000
		NOT = { original_tag = SWI }
		NOT = { original_tag = TIB }
		NOT = { original_tag = NEP }
		NOT = { original_tag = BHU }
		NOT = { original_tag = AFG }
		NOT = { original_tag = PER }
		NOT = { original_tag = AZB }
		NOT = { original_tag = ARM }
		NOT = { original_tag = GEO }
		NOT = { original_tag = ITA }
		NOT = { original_tag = AUS }
		NOT = { original_tag = NOR }
		NOT = { original_tag = PRU }
		NOT = { original_tag = CHL }
		NOT = { original_tag = OTT }
		NOT = { original_tag = YUN }
		NOT = { original_tag = XSM }
		NOT = { original_tag = SRI }
		NOT = { original_tag = ETH }
		NOT = { original_tag = MEX }
		NOT = { original_tag = COL }
		NOT = { original_tag = BOL }
		NOT = { original_tag = ARG }
		NOT = { original_tag = HUN }
		NOT = { original_tag = GRE }
		NOT = { original_tag = BUL }
		NOT = { original_tag = POR }
		NOT = { original_tag = SWE }
		NOT = { original_tag = PHL }
		NOT = { original_tag = USA }
		NOT = { original_tag = CSA }
		NOT = { original_tag = CAL }
		NOT = { original_tag = TEX }
		NOT = { original_tag = CUB }
		NOT = { original_tag = HAI }
		NOT = { original_tag = DOM }
		NOT = { original_tag = CAN }
		NOT = { original_tag = MAF }
		NOT = { original_tag = GER }
		NOT = { original_tag = AOG }
		NOT = { original_tag = CAF }
		NOT = { original_tag = NFA }
		NOT = { original_tag = JAP }
		NOT = { original_tag = TRM }
		NOT = { original_tag = AST }
		NOT = { original_tag = HOL }
		NOT = { original_tag = CRE }
		NOT = { original_tag = ENG }
		NOT = { original_tag = IRE }
		NOT = { original_tag = SPR }
		NOT = { original_tag = CAR }
		NOT = { original_tag = CNT }
		NOT = { original_tag = MON }
		NOT = { original_tag = ALO }
		NOT = { original_tag = TRK }
		NOT = { original_tag = DKB }
		NOT = { original_tag = SAU }
		NOT = { original_tag = YEM }
		NOT = { original_tag = OMA }
	}	
	abort = {  
		OR = {
			NOT = {has_army_size = { size < 50 } }
			has_war = yes
			NOT = { has_equipment = { infantry_equipment > 20000 }}
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 50	
	}
}

ai_army_building_medium_stockpile = { # Build if you're sitting on too many guns
	enable = {
		ai_wants_divisions < 100
		has_army_size = { size < 100 }
		has_equipment = { infantry_equipment > 40000 } 
		has_war = no
		has_manpower > 50000
		NOT = { original_tag = SWI }
		NOT = { original_tag = TIB }
		NOT = { original_tag = NEP }
		NOT = { original_tag = BHU }
		NOT = { original_tag = AFG }
		NOT = { original_tag = PER }
		NOT = { original_tag = AZB }
		NOT = { original_tag = ARM }
		NOT = { original_tag = GEO }
		NOT = { original_tag = ITA }
		NOT = { original_tag = AUS }
		NOT = { original_tag = NOR }
		NOT = { original_tag = PRU }
		NOT = { original_tag = CHL }
		NOT = { original_tag = OTT }
		NOT = { original_tag = YUN }
		NOT = { original_tag = XSM }
		NOT = { original_tag = SRI }
		NOT = { original_tag = ETH }
		NOT = { original_tag = MEX }
		NOT = { original_tag = COL }
		NOT = { original_tag = BOL }
		NOT = { original_tag = ARG }
		NOT = { original_tag = HUN }
		NOT = { original_tag = GRE }
		NOT = { original_tag = BUL }
		NOT = { original_tag = POR }
		NOT = { original_tag = SWE }
		NOT = { original_tag = PHL }
		NOT = { original_tag = USA }
		NOT = { original_tag = CSA }
		NOT = { original_tag = CAL }
		NOT = { original_tag = TEX }
		NOT = { original_tag = CUB }
		NOT = { original_tag = HAI }
		NOT = { original_tag = DOM }
		NOT = { original_tag = CAN }
		NOT = { original_tag = MAF }
		NOT = { original_tag = GER }
		NOT = { original_tag = AOG }
		NOT = { original_tag = CAF }
		NOT = { original_tag = NFA }
		NOT = { original_tag = JAP }
		NOT = { original_tag = TRM }
		NOT = { original_tag = AST }
		NOT = { original_tag = HOL }
		NOT = { original_tag = CRE }
		NOT = { original_tag = ENG }
		NOT = { original_tag = IRE }
		NOT = { original_tag = SPR }
		NOT = { original_tag = CAR }
		NOT = { original_tag = CNT }
		NOT = { original_tag = MON }
		NOT = { original_tag = ALO }
		NOT = { original_tag = TRK }
		NOT = { original_tag = DKB }
		NOT = { original_tag = SAU }
		NOT = { original_tag = YEM }
		NOT = { original_tag = OMA }
	}	
	abort = {  
		OR = {
			NOT = {has_army_size = { size < 100 } }
			has_war = yes
			NOT = { has_equipment = { infantry_equipment > 40000 }}
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 100	
	}
}

ai_army_building_large_stockpile = { # Build if you're sitting on too many guns
	enable = {
		ai_wants_divisions < 150
		has_army_size = { size < 150 }
		has_equipment = { infantry_equipment > 60000 } 
		has_war = no
		has_manpower > 50000
		NOT = { original_tag = SWI }
		NOT = { original_tag = TIB }
		NOT = { original_tag = NEP }
		NOT = { original_tag = BHU }
		NOT = { original_tag = AFG }
		NOT = { original_tag = PER }
		NOT = { original_tag = AZB }
		NOT = { original_tag = ARM }
		NOT = { original_tag = GEO }
		NOT = { original_tag = ITA }
		NOT = { original_tag = AUS }
		NOT = { original_tag = NOR }
		NOT = { original_tag = PRU }
		NOT = { original_tag = CHL }
		NOT = { original_tag = OTT }
		NOT = { original_tag = YUN }
		NOT = { original_tag = XSM }
		NOT = { original_tag = SRI }
		NOT = { original_tag = ETH }
		NOT = { original_tag = MEX }
		NOT = { original_tag = COL }
		NOT = { original_tag = BOL }
		NOT = { original_tag = ARG }
		NOT = { original_tag = HUN }
		NOT = { original_tag = GRE }
		NOT = { original_tag = BUL }
		NOT = { original_tag = POR }
		NOT = { original_tag = SWE }
		NOT = { original_tag = PHL }
		NOT = { original_tag = USA }
		NOT = { original_tag = CSA }
		NOT = { original_tag = CAL }
		NOT = { original_tag = TEX }
		NOT = { original_tag = CUB }
		NOT = { original_tag = HAI }
		NOT = { original_tag = DOM }
		NOT = { original_tag = CAN }
		NOT = { original_tag = MAF }
		NOT = { original_tag = GER }
		NOT = { original_tag = AOG }
		NOT = { original_tag = CAF }
		NOT = { original_tag = NFA }
		NOT = { original_tag = JAP }
		NOT = { original_tag = TRM }
		NOT = { original_tag = AST }
		NOT = { original_tag = HOL }
		NOT = { original_tag = CRE }
		NOT = { original_tag = ENG }
		NOT = { original_tag = IRE }
		NOT = { original_tag = SPR }
		NOT = { original_tag = CAR }
		NOT = { original_tag = CNT }
		NOT = { original_tag = MON }
		NOT = { original_tag = ALO }
		NOT = { original_tag = TRK }
		NOT = { original_tag = DKB }
		NOT = { original_tag = SAU }
		NOT = { original_tag = YEM }
		NOT = { original_tag = OMA }
	}	
	abort = {  
		OR = {
			NOT = {has_army_size = { size < 150 } }
			has_war = yes
			NOT = { has_equipment = { infantry_equipment > 60000 }}
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 150	
	}
}

########################
### Low Mountaineers ###
########################

ai_low_mon_army_building_minimum_puppet = { #Minimum Army Size Enforced - Puppets
	enable = {
		has_army_size = { size < 5 }
		is_subject = yes
		OR = {
			original_tag = OTT
			original_tag = YUN
			original_tag = XSM
			original_tag = ETH
			original_tag = MEX
			original_tag = COL
			original_tag = BOL
			original_tag = ARG
			original_tag = HUN
			original_tag = GRE
			original_tag = BUL
		}
	}	
	abort = {
		OR = {
			NOT = {has_army_size = { size < 5 } }
			has_war = yes
			is_subject = no
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 4		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 1	
	}
}

ai_low_mon_army_building_minimum = { #Minimum Army Size Enforced
	enable = {
		has_army_size = { size < 10 }
		has_war = no
		is_subject = no
		OR = {
			original_tag = OTT
			original_tag = YUN
			original_tag = XSM
			original_tag = ETH
			original_tag = MEX
			original_tag = COL
			original_tag = BOL
			original_tag = ARG
			original_tag = HUN
			original_tag = GRE
			original_tag = BUL
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 10 } }
			has_war = yes
			is_subject = yes
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 8		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 2	
	}
}

ai_low_mon_army_building_20 = { #20 Units
	enable = {
		ai_wants_divisions < 20
		has_army_size = { size < 20 }
		has_war = no
		num_of_factories > 9
		num_of_factories < 19
		has_manpower > 50000
		OR = {
			original_tag = OTT
			original_tag = YUN
			original_tag = XSM
			original_tag = ETH
			original_tag = MEX
			original_tag = COL
			original_tag = BOL
			original_tag = ARG
			original_tag = HUN
			original_tag = GRE
			original_tag = BUL
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 20 } }
			has_war = yes
			NOT = { num_of_factories > 9 }
			NOT = { num_of_factories < 19 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 16		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 4	
	}
}

ai_low_mon_army_building_30 = { #30 Units
	enable = {
		ai_wants_divisions < 30
		has_army_size = { size < 30 }
		has_war = no
		num_of_factories > 19
		num_of_factories < 29
		has_manpower > 50000
		OR = {
			original_tag = OTT
			original_tag = YUN
			original_tag = XSM
			original_tag = ETH
			original_tag = MEX
			original_tag = COL
			original_tag = BOL
			original_tag = ARG
			original_tag = HUN
			original_tag = GRE
			original_tag = BUL
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 30 } }
			has_war = yes
			NOT = { num_of_factories > 19 }
			NOT = { num_of_factories < 29 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 24		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 6	
	}
}

ai_low_mon_army_building_40 = { #40 Units
	enable = {
		ai_wants_divisions < 40
		has_army_size = { size < 40 }
		has_war = no
		num_of_factories > 29
		num_of_factories < 39
		has_manpower > 50000
		OR = {
			original_tag = OTT
			original_tag = YUN
			original_tag = XSM
			original_tag = ETH
			original_tag = MEX
			original_tag = COL
			original_tag = BOL
			original_tag = ARG
			original_tag = HUN
			original_tag = GRE
			original_tag = BUL
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 40 } }
			has_war = yes
			NOT = { num_of_factories > 29 }
			NOT = { num_of_factories < 39 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 32		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 8	
	}
}

ai_low_mon_army_building_50 = { #50 Units
	enable = {
		ai_wants_divisions < 50
		has_army_size = { size < 50 }
		has_war = no
		num_of_factories > 39
		num_of_factories < 49
		has_manpower > 50000
		OR = {
			original_tag = OTT
			original_tag = YUN
			original_tag = XSM
			original_tag = ETH
			original_tag = MEX
			original_tag = COL
			original_tag = BOL
			original_tag = ARG
			original_tag = HUN
			original_tag = GRE
			original_tag = BUL
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 50 } }
			has_war = yes
			NOT = { num_of_factories > 39 }
			NOT = { num_of_factories < 49 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 40		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 10	
	}
}

ai_low_mon_army_building_60 = { #60 Units
	enable = {
		ai_wants_divisions < 60
		has_army_size = { size < 60 }
		has_war = no
		num_of_factories > 49
		num_of_factories < 59
		has_manpower > 50000
		OR = {
			original_tag = OTT
			original_tag = YUN
			original_tag = XSM
			original_tag = ETH
			original_tag = MEX
			original_tag = COL
			original_tag = BOL
			original_tag = ARG
			original_tag = HUN
			original_tag = GRE
			original_tag = BUL
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 60 } }
			has_war = yes
			NOT = { num_of_factories > 49 }
			NOT = { num_of_factories < 59 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 48		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 12	
	}
}

ai_low_mon_army_building_70 = { #70 Units
	enable = {
		ai_wants_divisions < 70
		has_army_size = { size < 70 }
		has_war = no
		num_of_factories > 59
		num_of_factories < 69
		has_manpower > 50000
		OR = {
			original_tag = OTT
			original_tag = YUN
			original_tag = XSM
			original_tag = ETH
			original_tag = MEX
			original_tag = COL
			original_tag = BOL
			original_tag = ARG
			original_tag = HUN
			original_tag = GRE
			original_tag = BUL
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 70 } }
			has_war = yes
			NOT = { num_of_factories > 59 }
			NOT = { num_of_factories < 69 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 56		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 14	
	}
}

ai_low_mon_army_building_80 = { #80 Units
	enable = {
		ai_wants_divisions < 80
		has_army_size = { size < 80 }
		has_war = no
		num_of_factories > 69
		num_of_factories < 79
		has_manpower > 50000
		OR = {
			original_tag = OTT
			original_tag = YUN
			original_tag = XSM
			original_tag = ETH
			original_tag = MEX
			original_tag = COL
			original_tag = BOL
			original_tag = ARG
			original_tag = HUN
			original_tag = GRE
			original_tag = BUL
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 80 } }
			has_war = yes
			NOT = { num_of_factories > 69 }
			NOT = { num_of_factories < 79 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 64		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 16	
	}
}

ai_low_mon_army_building_90 = { #90 Units
	enable = {
		ai_wants_divisions < 90
		has_army_size = { size < 90 }
		has_war = no
		num_of_factories > 79
		num_of_factories < 89
		has_manpower > 50000
		OR = {
			original_tag = OTT
			original_tag = YUN
			original_tag = XSM
			original_tag = ETH
			original_tag = MEX
			original_tag = COL
			original_tag = BOL
			original_tag = ARG
			original_tag = HUN
			original_tag = GRE
			original_tag = BUL
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 90 } }
			has_war = yes
			NOT = { num_of_factories > 79 }
			NOT = { num_of_factories < 89 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 72		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 18	
	}
}

ai_low_mon_army_building_100 = { #100 Units
	enable = {
		ai_wants_divisions < 100
		has_army_size = { size < 100 }
		has_war = no
		num_of_factories > 89
		num_of_factories < 99
		has_manpower > 50000
		OR = {
			original_tag = OTT
			original_tag = YUN
			original_tag = XSM
			original_tag = ETH
			original_tag = MEX
			original_tag = COL
			original_tag = BOL
			original_tag = ARG
			original_tag = HUN
			original_tag = GRE
			original_tag = BUL
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 100 } }
			has_war = yes
			NOT = { num_of_factories > 89 }
			NOT = { num_of_factories < 99 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 80		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 20	
	}
}

ai_low_mon_army_building_150 = { #150 Units
	enable = {
		ai_wants_divisions < 150
		has_army_size = { size < 150 }
		has_war = no
		num_of_factories > 99
		has_manpower > 50000
		OR = {
			original_tag = OTT
			original_tag = YUN
			original_tag = XSM
			original_tag = ETH
			original_tag = MEX
			original_tag = COL
			original_tag = BOL
			original_tag = ARG
			original_tag = HUN
			original_tag = GRE
			original_tag = BUL
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 150 } }
			has_war = yes
			NOT = { num_of_factories > 99 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 120		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 30	
	}
}

ai_low_mon_army_building_300 = { # Please Stop Building Units
	enable = {
		NOT = {ai_wants_divisions < 300}
		NOT = {has_army_size = { size < 300 }}
		has_war = no
		num_of_factories > 99
		has_manpower > 50000
		OR = {
			original_tag = OTT
			original_tag = YUN
			original_tag = XSM
			original_tag = ETH
			original_tag = MEX
			original_tag = COL
			original_tag = BOL
			original_tag = ARG
			original_tag = HUN
			original_tag = GRE
			original_tag = BUL
		}
	}	
	abort = { 
		OR = {
			has_army_size = { size < 300 }
			NOT = {num_of_factories > 99}
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = -300		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = -300	
	}
}

ai_low_mon_army_building_small_stockpile = { # Build if you're sitting on too many guns
	enable = {
		ai_wants_divisions < 50
		has_army_size = { size < 50 }
		has_equipment = { infantry_equipment > 20000 } 
		has_war = no
		has_manpower > 50000
		OR = {
			original_tag = OTT
			original_tag = YUN
			original_tag = XSM
			original_tag = ETH
			original_tag = MEX
			original_tag = COL
			original_tag = BOL
			original_tag = ARG
			original_tag = HUN
			original_tag = GRE
			original_tag = BUL
		}
	}	
	abort = {  
		OR = {
			NOT = {has_army_size = { size < 50 } }
			has_war = yes
			NOT = { has_equipment = { infantry_equipment > 20000 }}
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 40	
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 10	
	}
}

ai_low_mon_army_building_medium_stockpile = { # Build if you're sitting on too many guns
	enable = {
		ai_wants_divisions < 100
		has_army_size = { size < 100 }
		has_equipment = { infantry_equipment > 40000 } 
		has_war = no
		has_manpower > 50000
		OR = {
			original_tag = OTT
			original_tag = YUN
			original_tag = XSM
			original_tag = ETH
			original_tag = MEX
			original_tag = COL
			original_tag = BOL
			original_tag = ARG
			original_tag = HUN
			original_tag = GRE
			original_tag = BUL
		}
	}	
	abort = {  
		OR = {
			NOT = {has_army_size = { size < 100 } }
			has_war = yes
			NOT = { has_equipment = { infantry_equipment > 40000 }}
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 80	
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 20	
	}
}

ai_low_mon_army_building_large_stockpile = { # Build if you're sitting on too many guns
	enable = {
		ai_wants_divisions < 150
		has_army_size = { size < 150 }
		has_equipment = { infantry_equipment > 60000 } 
		has_war = no
		has_manpower > 50000
		OR = {
			original_tag = OTT
			original_tag = YUN
			original_tag = XSM
			original_tag = ETH
			original_tag = MEX
			original_tag = COL
			original_tag = BOL
			original_tag = ARG
			original_tag = HUN
			original_tag = GRE
			original_tag = BUL
		}
	}	
	abort = {  
		OR = {
			NOT = {has_army_size = { size < 150 } }
			has_war = yes
			NOT = { has_equipment = { infantry_equipment > 60000 }}
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 120	
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 30	
	}
}


########################
### Medium Mountaineers ###
########################

ai_med_mon_army_building_minimum_puppet = { #Minimum Army Size Enforced - Puppets
	enable = {
		has_army_size = { size < 5 }
		is_subject = yes
		OR = {
			original_tag = AFG
			original_tag = PER
			original_tag = AZB
			original_tag = ARM
			original_tag = SRI
			original_tag = GEO
			original_tag = ITA
			original_tag = AUS
			original_tag = NOR
			original_tag = PRU
			original_tag = CHL
		}
	}	
	abort = {
		OR = {
			NOT = {has_army_size = { size < 5 } }
			has_war = yes
			is_subject = no
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 3		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 2	
	}
}

ai_med_mon_army_building_minimum = { #Minimum Army Size Enforced
	enable = {
		has_army_size = { size < 10 }
		has_war = no
		is_subject = no
		OR = {
			original_tag = AFG
			original_tag = PER
			original_tag = AZB
			original_tag = ARM
			original_tag = SRI
			original_tag = GEO
			original_tag = ITA
			original_tag = AUS
			original_tag = NOR
			original_tag = PRU
			original_tag = CHL
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 10 } }
			has_war = yes
			is_subject = yes
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 6		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 4	
	}
}

ai_med_mon_army_building_20 = { #20 Units
	enable = {
		ai_wants_divisions < 20
		has_army_size = { size < 20 }
		has_war = no
		num_of_factories > 9
		num_of_factories < 19
		has_manpower > 50000
		OR = {
			original_tag = AFG
			original_tag = PER
			original_tag = AZB
			original_tag = ARM
			original_tag = SRI
			original_tag = GEO
			original_tag = ITA
			original_tag = AUS
			original_tag = NOR
			original_tag = PRU
			original_tag = CHL
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 20 } }
			has_war = yes
			NOT = { num_of_factories > 9 }
			NOT = { num_of_factories < 19 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 12		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 8	
	}
}

ai_med_mon_army_building_30 = { #30 Units
	enable = {
		ai_wants_divisions < 30
		has_army_size = { size < 30 }
		has_war = no
		num_of_factories > 19
		num_of_factories < 29
		has_manpower > 50000
		OR = {
			original_tag = AFG
			original_tag = PER
			original_tag = AZB
			original_tag = ARM
			original_tag = SRI
			original_tag = GEO
			original_tag = ITA
			original_tag = AUS
			original_tag = NOR
			original_tag = PRU
			original_tag = CHL
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 30 } }
			has_war = yes
			NOT = { num_of_factories > 19 }
			NOT = { num_of_factories < 29 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 18		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 12	
	}
}

ai_med_mon_army_building_40 = { #40 Units
	enable = {
		ai_wants_divisions < 40
		has_army_size = { size < 40 }
		has_war = no
		num_of_factories > 29
		num_of_factories < 39
		has_manpower > 50000
		OR = {
			original_tag = AFG
			original_tag = PER
			original_tag = AZB
			original_tag = ARM
			original_tag = SRI
			original_tag = GEO
			original_tag = ITA
			original_tag = AUS
			original_tag = NOR
			original_tag = PRU
			original_tag = CHL
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 40 } }
			has_war = yes
			NOT = { num_of_factories > 29 }
			NOT = { num_of_factories < 39 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 24		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 16	
	}
}

ai_med_mon_army_building_50 = { #50 Units
	enable = {
		ai_wants_divisions < 50
		has_army_size = { size < 50 }
		has_war = no
		num_of_factories > 39
		num_of_factories < 49
		has_manpower > 50000
		OR = {
			original_tag = AFG
			original_tag = PER
			original_tag = AZB
			original_tag = ARM
			original_tag = SRI
			original_tag = GEO
			original_tag = ITA
			original_tag = AUS
			original_tag = NOR
			original_tag = PRU
			original_tag = CHL
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 50 } }
			has_war = yes
			NOT = { num_of_factories > 39 }
			NOT = { num_of_factories < 49 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 30		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 20	
	}
}

ai_med_mon_army_building_60 = { #60 Units
	enable = {
		ai_wants_divisions < 60
		has_army_size = { size < 60 }
		has_war = no
		num_of_factories > 49
		num_of_factories < 59
		has_manpower > 50000
		OR = {
			original_tag = AFG
			original_tag = PER
			original_tag = AZB
			original_tag = ARM
			original_tag = SRI
			original_tag = GEO
			original_tag = ITA
			original_tag = AUS
			original_tag = NOR
			original_tag = PRU
			original_tag = CHL
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 60 } }
			has_war = yes
			NOT = { num_of_factories > 49 }
			NOT = { num_of_factories < 59 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 36		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 24	
	}
}

ai_med_mon_army_building_70 = { #70 Units
	enable = {
		ai_wants_divisions < 70
		has_army_size = { size < 70 }
		has_war = no
		num_of_factories > 59
		num_of_factories < 69
		has_manpower > 50000
		OR = {
			original_tag = AFG
			original_tag = PER
			original_tag = AZB
			original_tag = ARM
			original_tag = SRI
			original_tag = GEO
			original_tag = ITA
			original_tag = AUS
			original_tag = NOR
			original_tag = PRU
			original_tag = CHL
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 70 } }
			has_war = yes
			NOT = { num_of_factories > 59 }
			NOT = { num_of_factories < 69 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 42		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 28	
	}
}

ai_med_mon_army_building_80 = { #80 Units
	enable = {
		ai_wants_divisions < 80
		has_army_size = { size < 80 }
		has_war = no
		num_of_factories > 69
		num_of_factories < 79
		has_manpower > 50000
		OR = {
			original_tag = AFG
			original_tag = PER
			original_tag = AZB
			original_tag = ARM
			original_tag = SRI
			original_tag = GEO
			original_tag = ITA
			original_tag = AUS
			original_tag = NOR
			original_tag = PRU
			original_tag = CHL
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 80 } }
			has_war = yes
			NOT = { num_of_factories > 69 }
			NOT = { num_of_factories < 79 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 48		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 32	
	}
}

ai_med_mon_army_building_90 = { #90 Units
	enable = {
		ai_wants_divisions < 90
		has_army_size = { size < 90 }
		has_war = no
		num_of_factories > 79
		num_of_factories < 89
		has_manpower > 50000
		OR = {
			original_tag = AFG
			original_tag = PER
			original_tag = AZB
			original_tag = ARM
			original_tag = SRI
			original_tag = GEO
			original_tag = ITA
			original_tag = AUS
			original_tag = NOR
			original_tag = PRU
			original_tag = CHL
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 90 } }
			has_war = yes
			NOT = { num_of_factories > 79 }
			NOT = { num_of_factories < 89 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 54		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 36	
	}
}

ai_med_mon_army_building_100 = { #100 Units
	enable = {
		ai_wants_divisions < 100
		has_army_size = { size < 100 }
		has_war = no
		num_of_factories > 89
		num_of_factories < 99
		has_manpower > 50000
		OR = {
			original_tag = AFG
			original_tag = PER
			original_tag = AZB
			original_tag = ARM
			original_tag = SRI
			original_tag = GEO
			original_tag = ITA
			original_tag = AUS
			original_tag = NOR
			original_tag = PRU
			original_tag = CHL
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 100 } }
			has_war = yes
			NOT = { num_of_factories > 89 }
			NOT = { num_of_factories < 99 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 60		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 40	
	}
}

ai_med_mon_army_building_150 = { #150 Units
	enable = {
		ai_wants_divisions < 150
		has_army_size = { size < 150 }
		has_war = no
		num_of_factories > 99
		has_manpower > 50000
		OR = {
			original_tag = AFG
			original_tag = PER
			original_tag = AZB
			original_tag = ARM
			original_tag = SRI
			original_tag = GEO
			original_tag = ITA
			original_tag = AUS
			original_tag = NOR
			original_tag = PRU
			original_tag = CHL
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 150 } }
			has_war = yes
			NOT = { num_of_factories > 99 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 90		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 60	
	}
}

ai_med_mon_army_building_300 = { # Please Stop Building Units
	enable = {
		NOT = {ai_wants_divisions < 300}
		NOT = {has_army_size = { size < 300 }}
		has_war = no
		num_of_factories > 99
		has_manpower > 50000
		OR = {
			original_tag = AFG
			original_tag = PER
			original_tag = AZB
			original_tag = ARM
			original_tag = SRI
			original_tag = GEO
			original_tag = ITA
			original_tag = AUS
			original_tag = NOR
			original_tag = PRU
			original_tag = CHL
		}
	}	
	abort = { 
		OR = {
			has_army_size = { size < 300 }
			NOT = {num_of_factories > 99}
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = -300		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = -300	
	}
}

ai_med_mon_army_building_small_stockpile = { # Build if you're sitting on too many guns
	enable = {
		ai_wants_divisions < 50
		has_army_size = { size < 50 }
		has_equipment = { infantry_equipment > 20000 } 
		has_war = no
		has_manpower > 50000
		OR = {
			original_tag = AFG
			original_tag = PER
			original_tag = AZB
			original_tag = ARM
			original_tag = SRI
			original_tag = GEO
			original_tag = ITA
			original_tag = AUS
			original_tag = NOR
			original_tag = PRU
			original_tag = CHL
		}
	}	
	abort = {  
		OR = {
			NOT = {has_army_size = { size < 50 } }
			has_war = yes
			NOT = { has_equipment = { infantry_equipment > 20000 }}
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 30	
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 20	
	}
}

ai_med_mon_army_building_medium_stockpile = { # Build if you're sitting on too many guns
	enable = {
		ai_wants_divisions < 100
		has_army_size = { size < 100 }
		has_equipment = { infantry_equipment > 40000 } 
		has_war = no
		has_manpower > 50000
		OR = {
			original_tag = AFG
			original_tag = PER
			original_tag = AZB
			original_tag = ARM
			original_tag = SRI
			original_tag = GEO
			original_tag = ITA
			original_tag = AUS
			original_tag = NOR
			original_tag = PRU
			original_tag = CHL
		}
	}	
	abort = {  
		OR = {
			NOT = {has_army_size = { size < 100 } }
			has_war = yes
			NOT = { has_equipment = { infantry_equipment > 40000 }}
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 60	
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 40	
	}
}

ai_med_mon_army_building_large_stockpile = { # Build if you're sitting on too many guns
	enable = {
		ai_wants_divisions < 150
		has_army_size = { size < 150 }
		has_equipment = { infantry_equipment > 60000 } 
		has_war = no
		has_manpower > 50000
		OR = {
			original_tag = AFG
			original_tag = PER
			original_tag = AZB
			original_tag = ARM
			original_tag = SRI
			original_tag = GEO
			original_tag = ITA
			original_tag = AUS
			original_tag = NOR
			original_tag = PRU
			original_tag = CHL
		}
	}	
	abort = {  
		OR = {
			NOT = {has_army_size = { size < 150 } }
			has_war = yes
			NOT = { has_equipment = { infantry_equipment > 60000 }}
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 90	
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 60	
	}
}


########################
### Extreme Mountaineers ###
########################

ai_ext_mon_army_building_minimum_puppet = { #Minimum Army Size Enforced - Puppets
	enable = {
		has_army_size = { size < 5 }
		is_subject = yes
		OR = {
			original_tag = SWI
			original_tag = TIB
			original_tag = NEP
			original_tag = BHU
		}
	}	
	abort = {
		OR = {
			NOT = {has_army_size = { size < 5 } }
			has_war = yes
			is_subject = no
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 2		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 3	
	}
}

ai_ext_mon_army_building_minimum = { #Minimum Army Size Enforced
	enable = {
		has_army_size = { size < 10 }
		has_war = no
		is_subject = no
		OR = {
			original_tag = SWI
			original_tag = TIB
			original_tag = NEP
			original_tag = BHU
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 10 } }
			has_war = yes
			is_subject = yes
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 4		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 6	
	}
}

ai_ext_mon_army_building_20 = { #20 Units
	enable = {
		ai_wants_divisions < 20
		has_army_size = { size < 20 }
		has_war = no
		num_of_factories > 9
		num_of_factories < 19
		has_manpower > 50000
		OR = {
			original_tag = SWI
			original_tag = TIB
			original_tag = NEP
			original_tag = BHU
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 20 } }
			has_war = yes
			NOT = { num_of_factories > 9 }
			NOT = { num_of_factories < 19 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 8		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 12	
	}
}

ai_ext_mon_army_building_30 = { #30 Units
	enable = {
		ai_wants_divisions < 30
		has_army_size = { size < 30 }
		has_war = no
		num_of_factories > 19
		num_of_factories < 29
		has_manpower > 50000
		OR = {
			original_tag = SWI
			original_tag = TIB
			original_tag = NEP
			original_tag = BHU
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 30 } }
			has_war = yes
			NOT = { num_of_factories > 19 }
			NOT = { num_of_factories < 29 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 12		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 18	
	}
}

ai_ext_mon_army_building_40 = { #40 Units
	enable = {
		ai_wants_divisions < 40
		has_army_size = { size < 40 }
		has_war = no
		num_of_factories > 29
		num_of_factories < 39
		has_manpower > 50000
		OR = {
			original_tag = SWI
			original_tag = TIB
			original_tag = NEP
			original_tag = BHU
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 40 } }
			has_war = yes
			NOT = { num_of_factories > 29 }
			NOT = { num_of_factories < 39 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 16		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 24	
	}
}

ai_ext_mon_army_building_50 = { #50 Units
	enable = {
		ai_wants_divisions < 50
		has_army_size = { size < 50 }
		has_war = no
		num_of_factories > 39
		num_of_factories < 49
		has_manpower > 50000
		OR = {
			original_tag = SWI
			original_tag = TIB
			original_tag = NEP
			original_tag = BHU
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 50 } }
			has_war = yes
			NOT = { num_of_factories > 39 }
			NOT = { num_of_factories < 49 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 20		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 30	
	}
}

ai_ext_mon_army_building_60 = { #60 Units
	enable = {
		ai_wants_divisions < 60
		has_army_size = { size < 60 }
		has_war = no
		num_of_factories > 49
		num_of_factories < 59
		has_manpower > 50000
		OR = {
			original_tag = SWI
			original_tag = TIB
			original_tag = NEP
			original_tag = BHU
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 60 } }
			has_war = yes
			NOT = { num_of_factories > 49 }
			NOT = { num_of_factories < 59 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 24		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 36	
	}
}

ai_ext_mon_army_building_70 = { #70 Units
	enable = {
		ai_wants_divisions < 70
		has_army_size = { size < 70 }
		has_war = no
		num_of_factories > 59
		num_of_factories < 69
		has_manpower > 50000
		OR = {
			original_tag = SWI
			original_tag = TIB
			original_tag = NEP
			original_tag = BHU
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 70 } }
			has_war = yes
			NOT = { num_of_factories > 59 }
			NOT = { num_of_factories < 69 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 28		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 42	
	}
}

ai_ext_mon_army_building_80 = { #80 Units
	enable = {
		ai_wants_divisions < 80
		has_army_size = { size < 80 }
		has_war = no
		num_of_factories > 69
		num_of_factories < 79
		has_manpower > 50000
		OR = {
			original_tag = SWI
			original_tag = TIB
			original_tag = NEP
			original_tag = BHU
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 80 } }
			has_war = yes
			NOT = { num_of_factories > 69 }
			NOT = { num_of_factories < 79 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 32		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 48	
	}
}

ai_ext_mon_army_building_90 = { #90 Units
	enable = {
		ai_wants_divisions < 90
		has_army_size = { size < 90 }
		has_war = no
		num_of_factories > 79
		num_of_factories < 89
		has_manpower > 50000
		OR = {
			original_tag = SWI
			original_tag = TIB
			original_tag = NEP
			original_tag = BHU
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 90 } }
			has_war = yes
			NOT = { num_of_factories > 79 }
			NOT = { num_of_factories < 89 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 36		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 54	
	}
}

ai_ext_mon_army_building_100 = { #100 Units
	enable = {
		ai_wants_divisions < 100
		has_army_size = { size < 100 }
		has_war = no
		num_of_factories > 89
		num_of_factories < 99
		has_manpower > 50000
		OR = {
			original_tag = SWI
			original_tag = TIB
			original_tag = NEP
			original_tag = BHU
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 100 } }
			has_war = yes
			NOT = { num_of_factories > 89 }
			NOT = { num_of_factories < 99 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 40		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 60	
	}
}

ai_ext_mon_army_building_150 = { #150 Units
	enable = {
		ai_wants_divisions < 150
		has_army_size = { size < 150 }
		has_war = no
		num_of_factories > 99
		has_manpower > 50000
		OR = {
			original_tag = SWI
			original_tag = TIB
			original_tag = NEP
			original_tag = BHU
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 150 } }
			has_war = yes
			NOT = { num_of_factories > 99 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 60		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 90	
	}
}

ai_ext_mon_army_building_300 = { # Please Stop Building Units
	enable = {
		NOT = {ai_wants_divisions < 300}
		NOT = {has_army_size = { size < 300 }}
		has_war = no
		num_of_factories > 99
		has_manpower > 50000
		OR = {
			original_tag = SWI
			original_tag = TIB
			original_tag = NEP
			original_tag = BHU
		}
	}	
	abort = { 
		OR = {
			has_army_size = { size < 300 }
			NOT = {num_of_factories > 99}
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = -300		
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = -300	
	}
}

ai_ext_mon_army_building_small_stockpile = { # Build if you're sitting on too many guns
	enable = {
		ai_wants_divisions < 50
		has_army_size = { size < 50 }
		has_equipment = { infantry_equipment > 20000 } 
		has_war = no
		has_manpower > 50000
		OR = {
			original_tag = SWI
			original_tag = TIB
			original_tag = NEP
			original_tag = BHU
		}
	}	
	abort = {  
		OR = {
			NOT = {has_army_size = { size < 50 } }
			has_war = yes
			NOT = { has_equipment = { infantry_equipment > 20000 }}
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 20	
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 30	
	}
}

ai_ext_mon_army_building_medium_stockpile = { # Build if you're sitting on too many guns
	enable = {
		ai_wants_divisions < 100
		has_army_size = { size < 100 }
		has_equipment = { infantry_equipment > 40000 } 
		has_war = no
		has_manpower > 50000
		OR = {
			original_tag = SWI
			original_tag = TIB
			original_tag = NEP
			original_tag = BHU
		}
	}	
	abort = {  
		OR = {
			NOT = {has_army_size = { size < 100 } }
			has_war = yes
			NOT = { has_equipment = { infantry_equipment > 40000 }}
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 40	
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 60	
	}
}

ai_ext_mon_army_building_large_stockpile = { # Build if you're sitting on too many guns
	enable = {
		ai_wants_divisions < 150
		has_army_size = { size < 150 }
		has_equipment = { infantry_equipment > 60000 } 
		has_war = no
		has_manpower > 50000
		OR = {
			original_tag = SWI
			original_tag = TIB
			original_tag = NEP
			original_tag = BHU
		}
	}	
	abort = {  
		OR = {
			NOT = {has_army_size = { size < 150 } }
			has_war = yes
			NOT = { has_equipment = { infantry_equipment > 60000 }}
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 60	
	}
	ai_strategy = { 
		type = build_army 
		id = mountaineers
		value = 90	
	}
}

########################
### Low Marines ###
########################

ai_low_mar_army_building_minimum_puppet = { #Minimum Army Size Enforced - Puppets
	enable = {
		has_army_size = { size < 5 }
		is_subject = yes
		OR = {
			original_tag = POR
			original_tag = SWE
			original_tag = PHL
			original_tag = USA
			original_tag = CSA
			original_tag = CAL
			original_tag = TEX
			original_tag = CUB
			original_tag = HAI
			original_tag = DOM
			original_tag = CAN
			original_tag = MAF
			original_tag = GER
			original_tag = AOG
		}
	}	
	abort = {
		OR = {
			NOT = {has_army_size = { size < 5 } }
			has_war = yes
			is_subject = no
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 4		
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = 1	
	}
}

ai_low_mar_army_building_minimum = { #Minimum Army Size Enforced
	enable = {
		has_army_size = { size < 10 }
		has_war = no
		is_subject = no
		OR = {
			original_tag = POR
			original_tag = SWE
			original_tag = PHL
			original_tag = USA
			original_tag = CSA
			original_tag = CAL
			original_tag = TEX
			original_tag = CUB
			original_tag = HAI
			original_tag = DOM
			original_tag = CAN
			original_tag = MAF
			original_tag = GER
			original_tag = AOG
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 10 } }
			has_war = yes
			is_subject = yes
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 9		
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = 1	
	}
}

ai_low_mar_army_building_20 = { #20 Units
	enable = {
		ai_wants_divisions < 20
		has_army_size = { size < 20 }
		has_war = no
		num_of_factories > 9
		num_of_factories < 19
		has_manpower > 50000
		OR = {
			original_tag = POR
			original_tag = SWE
			original_tag = PHL
			original_tag = USA
			original_tag = CSA
			original_tag = CAL
			original_tag = TEX
			original_tag = CUB
			original_tag = HAI
			original_tag = DOM
			original_tag = CAN
			original_tag = MAF
			original_tag = GER
			original_tag = AOG
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 20 } }
			has_war = yes
			NOT = { num_of_factories > 9 }
			NOT = { num_of_factories < 19 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 18		
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = 2	
	}
}

ai_low_mar_army_building_30 = { #30 Units
	enable = {
		ai_wants_divisions < 30
		has_army_size = { size < 30 }
		has_war = no
		num_of_factories > 19
		num_of_factories < 29
		has_manpower > 50000
		OR = {
			original_tag = POR
			original_tag = SWE
			original_tag = PHL
			original_tag = USA
			original_tag = CSA
			original_tag = CAL
			original_tag = TEX
			original_tag = CUB
			original_tag = HAI
			original_tag = DOM
			original_tag = CAN
			original_tag = MAF
			original_tag = GER
			original_tag = AOG
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 30 } }
			has_war = yes
			NOT = { num_of_factories > 19 }
			NOT = { num_of_factories < 29 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 27		
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = 3	
	}
}

ai_low_mar_army_building_40 = { #40 Units
	enable = {
		ai_wants_divisions < 40
		has_army_size = { size < 40 }
		has_war = no
		num_of_factories > 29
		num_of_factories < 39
		has_manpower > 50000
		OR = {
			original_tag = POR
			original_tag = SWE
			original_tag = PHL
			original_tag = USA
			original_tag = CSA
			original_tag = CAL
			original_tag = TEX
			original_tag = CUB
			original_tag = HAI
			original_tag = DOM
			original_tag = CAN
			original_tag = MAF
			original_tag = GER
			original_tag = AOG
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 40 } }
			has_war = yes
			NOT = { num_of_factories > 29 }
			NOT = { num_of_factories < 39 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 36		
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = 4	
	}
}

ai_low_mar_army_building_50 = { #50 Units
	enable = {
		ai_wants_divisions < 50
		has_army_size = { size < 50 }
		has_war = no
		num_of_factories > 39
		num_of_factories < 49
		has_manpower > 50000
		OR = {
			original_tag = POR
			original_tag = SWE
			original_tag = PHL
			original_tag = USA
			original_tag = CSA
			original_tag = CAL
			original_tag = TEX
			original_tag = CUB
			original_tag = HAI
			original_tag = DOM
			original_tag = CAN
			original_tag = MAF
			original_tag = GER
			original_tag = AOG
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 50 } }
			has_war = yes
			NOT = { num_of_factories > 39 }
			NOT = { num_of_factories < 49 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 45		
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = 5	
	}
}

ai_low_mar_army_building_60 = { #60 Units
	enable = {
		ai_wants_divisions < 60
		has_army_size = { size < 60 }
		has_war = no
		num_of_factories > 49
		num_of_factories < 59
		has_manpower > 50000
		OR = {
			original_tag = POR
			original_tag = SWE
			original_tag = PHL
			original_tag = USA
			original_tag = CSA
			original_tag = CAL
			original_tag = TEX
			original_tag = CUB
			original_tag = HAI
			original_tag = DOM
			original_tag = CAN
			original_tag = MAF
			original_tag = GER
			original_tag = AOG
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 60 } }
			has_war = yes
			NOT = { num_of_factories > 49 }
			NOT = { num_of_factories < 59 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 54		
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = 6	
	}
}

ai_low_mar_army_building_70 = { #70 Units
	enable = {
		ai_wants_divisions < 70
		has_army_size = { size < 70 }
		has_war = no
		num_of_factories > 59
		num_of_factories < 69
		has_manpower > 50000
		OR = {
			original_tag = POR
			original_tag = SWE
			original_tag = PHL
			original_tag = USA
			original_tag = CSA
			original_tag = CAL
			original_tag = TEX
			original_tag = CUB
			original_tag = HAI
			original_tag = DOM
			original_tag = CAN
			original_tag = MAF
			original_tag = GER
			original_tag = AOG
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 70 } }
			has_war = yes
			NOT = { num_of_factories > 59 }
			NOT = { num_of_factories < 69 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 63		
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = 7	
	}
}

ai_low_mar_army_building_80 = { #80 Units
	enable = {
		ai_wants_divisions < 80
		has_army_size = { size < 80 }
		has_war = no
		num_of_factories > 69
		num_of_factories < 79
		has_manpower > 50000
		OR = {
			original_tag = POR
			original_tag = SWE
			original_tag = PHL
			original_tag = USA
			original_tag = CSA
			original_tag = CAL
			original_tag = TEX
			original_tag = CUB
			original_tag = HAI
			original_tag = DOM
			original_tag = CAN
			original_tag = MAF
			original_tag = GER
			original_tag = AOG
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 80 } }
			has_war = yes
			NOT = { num_of_factories > 69 }
			NOT = { num_of_factories < 79 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 72		
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = 8	
	}
}

ai_low_mar_army_building_90 = { #90 Units
	enable = {
		ai_wants_divisions < 90
		has_army_size = { size < 90 }
		has_war = no
		num_of_factories > 79
		num_of_factories < 89
		has_manpower > 50000
		OR = {
			original_tag = POR
			original_tag = SWE
			original_tag = PHL
			original_tag = USA
			original_tag = CSA
			original_tag = CAL
			original_tag = TEX
			original_tag = CUB
			original_tag = HAI
			original_tag = DOM
			original_tag = CAN
			original_tag = MAF
			original_tag = GER
			original_tag = AOG
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 90 } }
			has_war = yes
			NOT = { num_of_factories > 79 }
			NOT = { num_of_factories < 89 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 81		
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = 9	
	}
}

ai_low_mar_army_building_100 = { #100 Units
	enable = {
		ai_wants_divisions < 100
		has_army_size = { size < 100 }
		has_war = no
		num_of_factories > 89
		num_of_factories < 99
		has_manpower > 50000
		OR = {
			original_tag = POR
			original_tag = SWE
			original_tag = PHL
			original_tag = USA
			original_tag = CSA
			original_tag = CAL
			original_tag = TEX
			original_tag = CUB
			original_tag = HAI
			original_tag = DOM
			original_tag = CAN
			original_tag = MAF
			original_tag = GER
			original_tag = AOG
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 100 } }
			has_war = yes
			NOT = { num_of_factories > 89 }
			NOT = { num_of_factories < 99 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 90		
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = 10	
	}
}

ai_low_mar_army_building_150 = { #150 Units
	enable = {
		ai_wants_divisions < 150
		has_army_size = { size < 150 }
		has_war = no
		num_of_factories > 99
		has_manpower > 50000
		OR = {
			original_tag = POR
			original_tag = SWE
			original_tag = PHL
			original_tag = USA
			original_tag = CSA
			original_tag = CAL
			original_tag = TEX
			original_tag = CUB
			original_tag = HAI
			original_tag = DOM
			original_tag = CAN
			original_tag = MAF
			original_tag = GER
			original_tag = AOG
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 150 } }
			has_war = yes
			NOT = { num_of_factories > 99 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 135		
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = 15	
	}
}

ai_low_mar_army_building_300 = { # Please Stop Building Units
	enable = {
		NOT = {ai_wants_divisions < 300}
		NOT = {has_army_size = { size < 300 }}
		has_war = no
		num_of_factories > 99
		has_manpower > 50000
		OR = {
			original_tag = POR
			original_tag = SWE
			original_tag = PHL
			original_tag = USA
			original_tag = CSA
			original_tag = CAL
			original_tag = TEX
			original_tag = CUB
			original_tag = HAI
			original_tag = DOM
			original_tag = CAN
			original_tag = MAF
			original_tag = GER
			original_tag = AOG
		}
	}	
	abort = { 
		OR = {
			has_army_size = { size < 300 }
			NOT = {num_of_factories > 99}
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = -300		
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = -300	
	}
}

ai_low_mar_army_building_small_stockpile = { # Build if you're sitting on too many guns
	enable = {
		ai_wants_divisions < 50
		has_army_size = { size < 50 }
		has_equipment = { infantry_equipment > 20000 } 
		has_war = no
		has_manpower > 50000
		OR = {
			original_tag = POR
			original_tag = SWE
			original_tag = PHL
			original_tag = USA
			original_tag = CSA
			original_tag = CAL
			original_tag = TEX
			original_tag = CUB
			original_tag = HAI
			original_tag = DOM
			original_tag = CAN
			original_tag = MAF
			original_tag = GER
			original_tag = AOG
		}
	}	
	abort = {  
		OR = {
			NOT = {has_army_size = { size < 50 } }
			has_war = yes
			NOT = { has_equipment = { infantry_equipment > 20000 }}
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 45	
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = 5	
	}
}

ai_low_mar_army_building_medium_stockpile = { # Build if you're sitting on too many guns
	enable = {
		ai_wants_divisions < 100
		has_army_size = { size < 100 }
		has_equipment = { infantry_equipment > 40000 } 
		has_war = no
		has_manpower > 50000
		OR = {
			original_tag = POR
			original_tag = SWE
			original_tag = PHL
			original_tag = USA
			original_tag = CSA
			original_tag = CAL
			original_tag = TEX
			original_tag = CUB
			original_tag = HAI
			original_tag = DOM
			original_tag = CAN
			original_tag = MAF
			original_tag = GER
			original_tag = AOG
		}
	}	
	abort = {  
		OR = {
			NOT = {has_army_size = { size < 100 } }
			has_war = yes
			NOT = { has_equipment = { infantry_equipment > 40000 }}
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 90	
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = 10	
	}
}

ai_low_mar_army_building_large_stockpile = { # Build if you're sitting on too many guns
	enable = {
		ai_wants_divisions < 150
		has_army_size = { size < 150 }
		has_equipment = { infantry_equipment > 60000 } 
		has_war = no
		has_manpower > 50000
		OR = {
			original_tag = POR
			original_tag = SWE
			original_tag = PHL
			original_tag = USA
			original_tag = CSA
			original_tag = CAL
			original_tag = TEX
			original_tag = CUB
			original_tag = HAI
			original_tag = DOM
			original_tag = CAN
			original_tag = MAF
			original_tag = GER
			original_tag = AOG
		}
	}	
	abort = {  
		OR = {
			NOT = {has_army_size = { size < 150 } }
			has_war = yes
			NOT = { has_equipment = { infantry_equipment > 60000 }}
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 135
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = 15	
	}
}


########################
### Medium Marines ###
########################

ai_med_mar_army_building_minimum_puppet = { #Minimum Army Size Enforced - Puppets
	enable = {
		has_army_size = { size < 5 }
		is_subject = yes
		OR = {
			original_tag = CAF
			original_tag = NFA
			original_tag = JAP
			original_tag = TRM
			original_tag = AST
			original_tag = HOL
			original_tag = CRE
			original_tag = ENG
			original_tag = IRE
			original_tag = SPR
			original_tag = CAR
			original_tag = CNT
		}
	}	
	abort = {
		OR = {
			NOT = {has_army_size = { size < 5 } }
			has_war = yes
			is_subject = no
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 4		
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = 1	
	}
}

ai_med_mar_army_building_minimum = { #Minimum Army Size Enforced
	enable = {
		has_army_size = { size < 10 }
		has_war = no
		is_subject = no
		OR = {
			original_tag = CAF
			original_tag = NFA
			original_tag = JAP
			original_tag = TRM
			original_tag = AST
			original_tag = HOL
			original_tag = CRE
			original_tag = ENG
			original_tag = IRE
			original_tag = SPR
			original_tag = CAR
			original_tag = CNT
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 10 } }
			has_war = yes
			is_subject = yes
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 8		
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = 2	
	}
}

ai_med_mar_army_building_20 = { #20 Units
	enable = {
		ai_wants_divisions < 20
		has_army_size = { size < 20 }
		has_war = no
		num_of_factories > 9
		num_of_factories < 19
		has_manpower > 50000
		OR = {
			original_tag = CAF
			original_tag = NFA
			original_tag = JAP
			original_tag = TRM
			original_tag = AST
			original_tag = HOL
			original_tag = CRE
			original_tag = ENG
			original_tag = IRE
			original_tag = SPR
			original_tag = CAR
			original_tag = CNT
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 20 } }
			has_war = yes
			NOT = { num_of_factories > 9 }
			NOT = { num_of_factories < 19 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 16		
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = 4	
	}
}

ai_med_mar_army_building_30 = { #30 Units
	enable = {
		ai_wants_divisions < 30
		has_army_size = { size < 30 }
		has_war = no
		num_of_factories > 19
		num_of_factories < 29
		has_manpower > 50000
		OR = {
			original_tag = CAF
			original_tag = NFA
			original_tag = JAP
			original_tag = TRM
			original_tag = AST
			original_tag = HOL
			original_tag = CRE
			original_tag = ENG
			original_tag = IRE
			original_tag = SPR
			original_tag = CAR
			original_tag = CNT
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 30 } }
			has_war = yes
			NOT = { num_of_factories > 19 }
			NOT = { num_of_factories < 29 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 24		
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = 6	
	}
}

ai_med_mar_army_building_40 = { #40 Units
	enable = {
		ai_wants_divisions < 40
		has_army_size = { size < 40 }
		has_war = no
		num_of_factories > 29
		num_of_factories < 39
		has_manpower > 50000
		OR = {
			original_tag = CAF
			original_tag = NFA
			original_tag = JAP
			original_tag = TRM
			original_tag = AST
			original_tag = HOL
			original_tag = CRE
			original_tag = ENG
			original_tag = IRE
			original_tag = SPR
			original_tag = CAR
			original_tag = CNT
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 40 } }
			has_war = yes
			NOT = { num_of_factories > 29 }
			NOT = { num_of_factories < 39 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 32		
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = 8	
	}
}

ai_med_mar_army_building_50 = { #50 Units
	enable = {
		ai_wants_divisions < 50
		has_army_size = { size < 50 }
		has_war = no
		num_of_factories > 39
		num_of_factories < 49
		has_manpower > 50000
		OR = {
			original_tag = CAF
			original_tag = NFA
			original_tag = JAP
			original_tag = TRM
			original_tag = AST
			original_tag = HOL
			original_tag = CRE
			original_tag = ENG
			original_tag = IRE
			original_tag = SPR
			original_tag = CAR
			original_tag = CNT
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 50 } }
			has_war = yes
			NOT = { num_of_factories > 39 }
			NOT = { num_of_factories < 49 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 40		
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = 10	
	}
}

ai_med_mar_army_building_60 = { #60 Units
	enable = {
		ai_wants_divisions < 60
		has_army_size = { size < 60 }
		has_war = no
		num_of_factories > 49
		num_of_factories < 59
		has_manpower > 50000
		OR = {
			original_tag = CAF
			original_tag = NFA
			original_tag = JAP
			original_tag = TRM
			original_tag = AST
			original_tag = HOL
			original_tag = CRE
			original_tag = ENG
			original_tag = IRE
			original_tag = SPR
			original_tag = CAR
			original_tag = CNT
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 60 } }
			has_war = yes
			NOT = { num_of_factories > 49 }
			NOT = { num_of_factories < 59 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 48		
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = 12	
	}
}

ai_med_mar_army_building_70 = { #70 Units
	enable = {
		ai_wants_divisions < 70
		has_army_size = { size < 70 }
		has_war = no
		num_of_factories > 59
		num_of_factories < 69
		has_manpower > 50000
		OR = {
			original_tag = CAF
			original_tag = NFA
			original_tag = JAP
			original_tag = TRM
			original_tag = AST
			original_tag = HOL
			original_tag = CRE
			original_tag = ENG
			original_tag = IRE
			original_tag = SPR
			original_tag = CAR
			original_tag = CNT
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 70 } }
			has_war = yes
			NOT = { num_of_factories > 59 }
			NOT = { num_of_factories < 69 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 56		
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = 14	
	}
}

ai_med_mar_army_building_80 = { #80 Units
	enable = {
		ai_wants_divisions < 80
		has_army_size = { size < 80 }
		has_war = no
		num_of_factories > 69
		num_of_factories < 79
		has_manpower > 50000
		OR = {
			original_tag = CAF
			original_tag = NFA
			original_tag = JAP
			original_tag = TRM
			original_tag = AST
			original_tag = HOL
			original_tag = CRE
			original_tag = ENG
			original_tag = IRE
			original_tag = SPR
			original_tag = CAR
			original_tag = CNT
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 80 } }
			has_war = yes
			NOT = { num_of_factories > 69 }
			NOT = { num_of_factories < 79 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 64		
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = 16	
	}
}

ai_med_mar_army_building_90 = { #90 Units
	enable = {
		ai_wants_divisions < 90
		has_army_size = { size < 90 }
		has_war = no
		num_of_factories > 79
		num_of_factories < 89
		has_manpower > 50000
		OR = {
			original_tag = CAF
			original_tag = NFA
			original_tag = JAP
			original_tag = TRM
			original_tag = AST
			original_tag = HOL
			original_tag = CRE
			original_tag = ENG
			original_tag = IRE
			original_tag = SPR
			original_tag = CAR
			original_tag = CNT
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 90 } }
			has_war = yes
			NOT = { num_of_factories > 79 }
			NOT = { num_of_factories < 89 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 72		
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = 18	
	}
}

ai_med_mar_army_building_100 = { #100 Units
	enable = {
		ai_wants_divisions < 100
		has_army_size = { size < 100 }
		has_war = no
		num_of_factories > 89
		num_of_factories < 99
		has_manpower > 50000
		OR = {
			original_tag = CAF
			original_tag = NFA
			original_tag = JAP
			original_tag = TRM
			original_tag = AST
			original_tag = HOL
			original_tag = CRE
			original_tag = ENG
			original_tag = IRE
			original_tag = SPR
			original_tag = CAR
			original_tag = CNT
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 100 } }
			has_war = yes
			NOT = { num_of_factories > 89 }
			NOT = { num_of_factories < 99 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 80		
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = 20	
	}
}

ai_med_mar_army_building_150 = { #150 Units
	enable = {
		ai_wants_divisions < 150
		has_army_size = { size < 150 }
		has_war = no
		num_of_factories > 99
		has_manpower > 50000
		OR = {
			original_tag = CAF
			original_tag = NFA
			original_tag = JAP
			original_tag = TRM
			original_tag = AST
			original_tag = HOL
			original_tag = CRE
			original_tag = ENG
			original_tag = IRE
			original_tag = SPR
			original_tag = CAR
			original_tag = CNT
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 150 } }
			has_war = yes
			NOT = { num_of_factories > 99 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 120		
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = 30	
	}
}

ai_med_mar_army_building_300 = { # Please Stop Building Units
	enable = {
		NOT = {ai_wants_divisions < 300}
		NOT = {has_army_size = { size < 300 }}
		has_war = no
		num_of_factories > 99
		has_manpower > 50000
		OR = {
			original_tag = CAF
			original_tag = NFA
			original_tag = JAP
			original_tag = TRM
			original_tag = AST
			original_tag = HOL
			original_tag = CRE
			original_tag = ENG
			original_tag = IRE
			original_tag = SPR
			original_tag = CAR
			original_tag = CNT
		}
	}	
	abort = { 
		OR = {
			has_army_size = { size < 300 }
			NOT = {num_of_factories > 99}
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = -300		
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = -300	
	}
}

ai_med_mar_army_building_small_stockpile = { # Build if you're sitting on too many guns
	enable = {
		ai_wants_divisions < 50
		has_army_size = { size < 50 }
		has_equipment = { infantry_equipment > 20000 } 
		has_war = no
		has_manpower > 50000
		OR = {
			original_tag = CAF
			original_tag = NFA
			original_tag = JAP
			original_tag = TRM
			original_tag = AST
			original_tag = HOL
			original_tag = CRE
			original_tag = ENG
			original_tag = IRE
			original_tag = SPR
			original_tag = CAR
			original_tag = CNT
		}
	}	
	abort = {  
		OR = {
			NOT = {has_army_size = { size < 50 } }
			has_war = yes
			NOT = { has_equipment = { infantry_equipment > 20000 }}
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 40	
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = 10	
	}
}

ai_med_mar_army_building_medium_stockpile = { # Build if you're sitting on too many guns
	enable = {
		ai_wants_divisions < 100
		has_army_size = { size < 100 }
		has_equipment = { infantry_equipment > 40000 } 
		has_war = no
		has_manpower > 50000
		OR = {
			original_tag = CAF
			original_tag = NFA
			original_tag = JAP
			original_tag = TRM
			original_tag = AST
			original_tag = HOL
			original_tag = CRE
			original_tag = ENG
			original_tag = IRE
			original_tag = SPR
			original_tag = CAR
			original_tag = CNT
		}
	}	
	abort = {  
		OR = {
			NOT = {has_army_size = { size < 100 } }
			has_war = yes
			NOT = { has_equipment = { infantry_equipment > 40000 }}
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 80	
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = 20	
	}
}

ai_med_mar_army_building_large_stockpile = { # Build if you're sitting on too many guns
	enable = {
		ai_wants_divisions < 150
		has_army_size = { size < 150 }
		has_equipment = { infantry_equipment > 60000 } 
		has_war = no
		has_manpower > 50000
		OR = {
			original_tag = CAF
			original_tag = NFA
			original_tag = JAP
			original_tag = TRM
			original_tag = AST
			original_tag = HOL
			original_tag = CRE
			original_tag = ENG
			original_tag = IRE
			original_tag = SPR
			original_tag = CAR
			original_tag = CNT
		}
	}	
	abort = {  
		OR = {
			NOT = {has_army_size = { size < 150 } }
			has_war = yes
			NOT = { has_equipment = { infantry_equipment > 60000 }}
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 120	
	}
	ai_strategy = { 
		type = build_army 
		id = marines
		value = 30	
	}
}

########################
### Medium Cavalry ###
########################

ai_med_cav_army_building_minimum_puppet = { #Minimum Army Size Enforced - Puppets
	enable = {
		has_army_size = { size < 5 }
		is_subject = yes
		OR = {
			original_tag = MON
			original_tag = ALO
			original_tag = TRK
			original_tag = DKB
			original_tag = SAU
			original_tag = YEM
			original_tag = OMA
		}
	}	
	abort = {
		OR = {
			NOT = {has_army_size = { size < 5 } }
			has_war = yes
			is_subject = no
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 4		
	}
	ai_strategy = { 
		type = build_army 
		id = cavalry
		value = 1	
	}
}

ai_med_cav_army_building_minimum = { #Minimum Army Size Enforced
	enable = {
		has_army_size = { size < 10 }
		has_war = no
		is_subject = no
		OR = {
			original_tag = MON
			original_tag = ALO
			original_tag = TRK
			original_tag = DKB
			original_tag = SAU
			original_tag = YEM
			original_tag = OMA
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 10 } }
			has_war = yes
			is_subject = yes
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 8		
	}
	ai_strategy = { 
		type = build_army 
		id = cavalry
		value = 2	
	}
}

ai_med_cav_army_building_20 = { #20 Units
	enable = {
		ai_wants_divisions < 20
		has_army_size = { size < 20 }
		has_war = no
		num_of_factories > 9
		num_of_factories < 19
		has_manpower > 50000
		OR = {
			original_tag = MON
			original_tag = ALO
			original_tag = TRK
			original_tag = DKB
			original_tag = SAU
			original_tag = YEM
			original_tag = OMA
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 20 } }
			has_war = yes
			NOT = { num_of_factories > 9 }
			NOT = { num_of_factories < 19 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 16		
	}
	ai_strategy = { 
		type = build_army 
		id = cavalry
		value = 4	
	}
}

ai_med_cav_army_building_30 = { #30 Units
	enable = {
		ai_wants_divisions < 30
		has_army_size = { size < 30 }
		has_war = no
		num_of_factories > 19
		num_of_factories < 29
		has_manpower > 50000
		OR = {
			original_tag = MON
			original_tag = ALO
			original_tag = TRK
			original_tag = DKB
			original_tag = SAU
			original_tag = YEM
			original_tag = OMA
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 30 } }
			has_war = yes
			NOT = { num_of_factories > 19 }
			NOT = { num_of_factories < 29 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 24		
	}
	ai_strategy = { 
		type = build_army 
		id = cavalry
		value = 6	
	}
}

ai_med_cav_army_building_40 = { #40 Units
	enable = {
		ai_wants_divisions < 40
		has_army_size = { size < 40 }
		has_war = no
		num_of_factories > 29
		num_of_factories < 39
		has_manpower > 50000
		OR = {
			original_tag = MON
			original_tag = ALO
			original_tag = TRK
			original_tag = DKB
			original_tag = SAU
			original_tag = YEM
			original_tag = OMA
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 40 } }
			has_war = yes
			NOT = { num_of_factories > 29 }
			NOT = { num_of_factories < 39 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 32		
	}
	ai_strategy = { 
		type = build_army 
		id = cavalry
		value = 8	
	}
}

ai_med_cav_army_building_50 = { #50 Units
	enable = {
		ai_wants_divisions < 50
		has_army_size = { size < 50 }
		has_war = no
		num_of_factories > 39
		num_of_factories < 49
		has_manpower > 50000
		OR = {
			original_tag = MON
			original_tag = ALO
			original_tag = TRK
			original_tag = DKB
			original_tag = SAU
			original_tag = YEM
			original_tag = OMA
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 50 } }
			has_war = yes
			NOT = { num_of_factories > 39 }
			NOT = { num_of_factories < 49 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 40		
	}
	ai_strategy = { 
		type = build_army 
		id = cavalry
		value = 10	
	}
}

ai_med_cav_army_building_60 = { #60 Units
	enable = {
		ai_wants_divisions < 60
		has_army_size = { size < 60 }
		has_war = no
		num_of_factories > 49
		num_of_factories < 59
		has_manpower > 50000
		OR = {
			original_tag = MON
			original_tag = ALO
			original_tag = TRK
			original_tag = DKB
			original_tag = SAU
			original_tag = YEM
			original_tag = OMA
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 60 } }
			has_war = yes
			NOT = { num_of_factories > 49 }
			NOT = { num_of_factories < 59 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 48		
	}
	ai_strategy = { 
		type = build_army 
		id = cavalry
		value = 12	
	}
}

ai_med_cav_army_building_70 = { #70 Units
	enable = {
		ai_wants_divisions < 70
		has_army_size = { size < 70 }
		has_war = no
		num_of_factories > 59
		num_of_factories < 69
		has_manpower > 50000
		OR = {
			original_tag = MON
			original_tag = ALO
			original_tag = TRK
			original_tag = DKB
			original_tag = SAU
			original_tag = YEM
			original_tag = OMA
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 70 } }
			has_war = yes
			NOT = { num_of_factories > 59 }
			NOT = { num_of_factories < 69 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 56		
	}
	ai_strategy = { 
		type = build_army 
		id = cavalry
		value = 14	
	}
}

ai_med_cav_army_building_80 = { #80 Units
	enable = {
		ai_wants_divisions < 80
		has_army_size = { size < 80 }
		has_war = no
		num_of_factories > 69
		num_of_factories < 79
		has_manpower > 50000
		OR = {
			original_tag = MON
			original_tag = ALO
			original_tag = TRK
			original_tag = DKB
			original_tag = SAU
			original_tag = YEM
			original_tag = OMA
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 80 } }
			has_war = yes
			NOT = { num_of_factories > 69 }
			NOT = { num_of_factories < 79 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 64		
	}
	ai_strategy = { 
		type = build_army 
		id = cavalry
		value = 16	
	}
}

ai_med_cav_army_building_90 = { #90 Units
	enable = {
		ai_wants_divisions < 90
		has_army_size = { size < 90 }
		has_war = no
		num_of_factories > 79
		num_of_factories < 89
		has_manpower > 50000
		OR = {
			original_tag = MON
			original_tag = ALO
			original_tag = TRK
			original_tag = DKB
			original_tag = SAU
			original_tag = YEM
			original_tag = OMA
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 90 } }
			has_war = yes
			NOT = { num_of_factories > 79 }
			NOT = { num_of_factories < 89 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 72		
	}
	ai_strategy = { 
		type = build_army 
		id = cavalry
		value = 18	
	}
}

ai_med_cav_army_building_100 = { #100 Units
	enable = {
		ai_wants_divisions < 100
		has_army_size = { size < 100 }
		has_war = no
		num_of_factories > 89
		num_of_factories < 99
		has_manpower > 50000
		OR = {
			original_tag = MON
			original_tag = ALO
			original_tag = TRK
			original_tag = DKB
			original_tag = SAU
			original_tag = YEM
			original_tag = OMA
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 100 } }
			has_war = yes
			NOT = { num_of_factories > 89 }
			NOT = { num_of_factories < 99 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 80		
	}
	ai_strategy = { 
		type = build_army 
		id = cavalry
		value = 20	
	}
}

ai_med_cav_army_building_150 = { #150 Units
	enable = {
		ai_wants_divisions < 150
		has_army_size = { size < 150 }
		has_war = no
		num_of_factories > 99
		has_manpower > 50000
		OR = {
			original_tag = MON
			original_tag = ALO
			original_tag = TRK
			original_tag = DKB
			original_tag = SAU
			original_tag = YEM
			original_tag = OMA
		}
	}	
	abort = { 
		OR = {
			NOT = {has_army_size = { size < 150 } }
			has_war = yes
			NOT = { num_of_factories > 99 }
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 120		
	}
	ai_strategy = { 
		type = build_army 
		id = cavalry
		value = 30	
	}
}

ai_med_cav_army_building_300 = { # Please Stop Building Units
	enable = {
		NOT = {ai_wants_divisions < 300}
		NOT = {has_army_size = { size < 300 }}
		has_war = no
		num_of_factories > 99
		has_manpower > 50000
		OR = {
			original_tag = MON
			original_tag = ALO
			original_tag = TRK
			original_tag = DKB
			original_tag = SAU
			original_tag = YEM
			original_tag = OMA
		}
	}	
	abort = { 
		OR = {
			has_army_size = { size < 300 }
			NOT = {num_of_factories > 99}
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = -300		
	}
	ai_strategy = { 
		type = build_army 
		id = cavalry
		value = -300	
	}
}

ai_med_cav_army_building_small_stockpile = { # Build if you're sitting on too many guns
	enable = {
		ai_wants_divisions < 50
		has_army_size = { size < 50 }
		has_equipment = { infantry_equipment > 20000 } 
		has_war = no
		has_manpower > 50000
		OR = {
			original_tag = MON
			original_tag = ALO
			original_tag = TRK
			original_tag = DKB
			original_tag = SAU
			original_tag = YEM
			original_tag = OMA
		}
	}	
	abort = {  
		OR = {
			NOT = {has_army_size = { size < 50 } }
			has_war = yes
			NOT = { has_equipment = { infantry_equipment > 20000 }}
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 40	
	}
	ai_strategy = { 
		type = build_army 
		id = cavalry
		value = 10	
	}
}

ai_med_cav_army_building_medium_stockpile = { # Build if you're sitting on too many guns
	enable = {
		ai_wants_divisions < 100
		has_army_size = { size < 100 }
		has_equipment = { infantry_equipment > 40000 } 
		has_war = no
		has_manpower > 50000
		OR = {
			original_tag = MON
			original_tag = ALO
			original_tag = TRK
			original_tag = DKB
			original_tag = SAU
			original_tag = YEM
			original_tag = OMA
		}
	}	
	abort = {  
		OR = {
			NOT = {has_army_size = { size < 100 } }
			has_war = yes
			NOT = { has_equipment = { infantry_equipment > 40000 }}
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 80	
	}
	ai_strategy = { 
		type = build_army 
		id = cavalry
		value = 20	
	}
}

ai_med_cav_army_building_large_stockpile = { # Build if you're sitting on too many guns
	enable = {
		ai_wants_divisions < 150
		has_army_size = { size < 150 }
		has_equipment = { infantry_equipment > 60000 } 
		has_war = no
		has_manpower > 50000
		OR = {
			original_tag = MON
			original_tag = ALO
			original_tag = TRK
			original_tag = DKB
			original_tag = SAU
			original_tag = YEM
			original_tag = OMA
		}
	}	
	abort = {  
		OR = {
			NOT = {has_army_size = { size < 150 } }
			has_war = yes
			NOT = { has_equipment = { infantry_equipment > 60000 }}
			has_manpower < 1000
		}
	}
	ai_strategy = { 
		type = build_army 
		id = infantry
		value = 120	
	}
	ai_strategy = { 
		type = build_army 
		id = cavalry
		value = 30	
	}
}